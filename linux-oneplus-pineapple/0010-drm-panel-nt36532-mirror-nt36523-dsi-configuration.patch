--- a/drivers/gpu/drm/panel/panel-novatek-nt36532.c	2026-08-11 13:49:13.000329829 +0200
+++ b/drivers/gpu/drm/panel/panel-novatek-nt36532.c	2026-08-20 07:16:38.263654771 +0200
@@ -4,49 +4,121 @@
  * Based on the template generated by linux-mdss-dsi-panel-driver-generator
  *
  * Copyright (c) 2025 Junjie Cao <caojunjie650@gmail.com>
+ * Copyright (c) 2026 Serhii Hatsan <serhiihatcan@gmail.com>
  */
 
+#include <linux/backlight.h>
 #include <linux/delay.h>
+// #include <linux/firmware.h>
 #include <linux/gpio/consumer.h>
 #include <linux/mod_devicetable.h>
 #include <linux/module.h>
 #include <linux/of.h>
 #include <linux/of_graph.h>
 #include <linux/regulator/consumer.h>
+#include <linux/spi/spi.h>
+#include <linux/unaligned.h>
 
 #include <video/mipi_display.h>
 
 #include <drm/display/drm_dsc.h>
 #include <drm/display/drm_dsc_helper.h>
+#include <drm/drm_connector.h>
+#include <drm/drm_crtc.h>
 #include <drm/drm_mipi_dsi.h>
 #include <drm/drm_modes.h>
 #include <drm/drm_panel.h>
 #include <drm/drm_probe_helper.h>
 
-struct nt36532 {
+#define DSI_NUM_MIN 1
+
+// Firmware defines
+// #define FW_BUF_SIZE (256 * 1024) // 256KB
+// #define NVT_ILM_DES_ADDR 0x1FB528 // 0x3F128
+// #define NVT_DLM_DES_ADDR 0x1FB52C // 0x3F12C
+// #define NVT_ILM_LENGTH_ADDR 0x1FB518 // 0x3F118
+// #define NVT_DLM_LENGTH_ADDR 0x1FB530 // 0x3F130
+// #define NVT_G_ILM_CHECKSUM_ADDR 0x1FB500 // 0x3F100
+// #define NVT_G_DLM_CHECKSUM_ADDR 0x1FB504 // 0x3F104
+// #define NVT_EVENT_BUF_ADDR 0x125800 // 0x2FE00
+// #define NVT_BOOT_RDY_ADDR 0x1FB50D // 0x3F10D
+// #define NVT_TX_AUTO_COPY_EN 0x1FC925 // 0x3F7E8
+// #define NVT_SPI_DMA_TX_INFO 0x1FC914 // 0x3F7F1
+// #define NVT_SWRST_SIF_ADDR 0x1FB43E // 0x3F0FE
+// #define NVT_DMA_CRC_FLAG_ADDR 0x1FB534
+// #define NVT_BLD_ILM_DLM_CRC_ADDR 0x1FB533 // 0x3F133
+// #define NVT_R_ILM_CHECKSUM_ADDR 0x1FB520 // 0x3F120
+// #define NVT_R_DLM_CHECKSUM_ADDR 0x1FB524 // 0x3F124
+// #define NVT_TRANSFER_LEN 128
+// #define FW_BIN_CHECKSUM_LEN 4
+// #define EVENT_MAP_RESET_COMPLETE 0x60
+// #define EVENT_MAP_HOST_CMD 0x50
+// #define RESET_STATE_INIT 0xA0 // IC reset
+// #define RESET_STATE_MAX 0xAF
+// #define MAX_PARTITIONS 16
+
+static uint dsc_slice_count = 2;
+module_param(dsc_slice_count, uint, 0644);
+
+static uint dsc_slice_width = 750;
+module_param(dsc_slice_width, uint, 0644);
+
+static uint dsc_slice_per_pkt = 2;
+module_param(dsc_slice_per_pkt, uint, 0644);
+
+static uint dsc_version_minor = 2;
+module_param(dsc_version_minor, uint, 0644);
+
+static uint dsc_slice_height = 20;
+module_param(dsc_slice_height, uint, 0644);
+
+struct panel_info {
     struct drm_panel panel;
     struct mipi_dsi_device* dsi[2];
-    const struct panel_info* panel_info;
+    const struct panel_desc* desc;
+    enum drm_panel_orientation orientation;
     struct gpio_desc* reset_gpio;
+    // struct gpio_desc* mcu_reset_gpio;
+    struct backlight_device* backlight;
     struct regulator_bulk_data* supplies;
+    // struct spi_device* touch_spi;
 };
 
-struct panel_info {
+struct panel_desc {
     unsigned int width_mm;
     unsigned int height_mm;
 
+    unsigned int bpc;
     unsigned int lanes;
-    enum mipi_dsi_pixel_format format;
     unsigned long mode_flags;
+    enum mipi_dsi_pixel_format format;
 
-    const struct drm_display_mode display_mode;
+    const struct drm_display_mode* modes;
+    unsigned int num_modes;
+    const struct mipi_dsi_device_info dsi_info;
+
+    int (*init_sequence)(struct panel_info* pinfo);
 
     unsigned int dsc_slice_per_pkt;
     struct drm_dsc_config* const dsc_cfg;
-
-    int (*init_sequence)(struct nt36532* ctx);
+    unsigned int hs_rate;
 
     bool is_dual_dsi;
+    bool has_dcs_backlight; // Panel does not have it. Controlled by ktz8866
+//};
+
+//struct nvt_partition {
+//    char name[12];
+//    uint32_t sram_addr;
+//    uint32_t bin_addr;
+//    uint32_t size;
+//    uint32_t crc;
+//};
+//
+//struct fw_header {
+//    struct nvt_partition parts[MAX_PARTITIONS];
+//    uint32_t num_partitions;
+//    bool is_cascade;
 };
 
 static const struct regulator_bulk_data nt36532_supplies[] = {
@@ -55,125 +127,1793 @@
     { .supply = "vci" }, /* 3.0V Analog */
 };
 
-static inline struct nt36532* to_nt36532(struct drm_panel* panel)
+static inline struct panel_info* to_panel_info(struct drm_panel* panel)
 {
-    return container_of(panel, struct nt36532, panel);
+    return container_of(panel, struct panel_info, panel);
 }
 
-static void nt36532_reset(struct nt36532* ctx)
+static void nt36532_reset(struct panel_info* pinfo)
 {
-    gpiod_set_value_cansleep(ctx->reset_gpio, 0);
+    gpiod_set_value_cansleep(pinfo->reset_gpio, 1);
+    // gpiod_set_value_cansleep(pinfo->mcu_reset_gpio, 1);
     usleep_range(10000, 10100);
-    gpiod_set_value_cansleep(ctx->reset_gpio, 1);
+    gpiod_set_value_cansleep(pinfo->reset_gpio, 0);
+    // gpiod_set_value_cansleep(pinfo->mcu_reset_gpio, 0);
     usleep_range(5000, 5100);
-    gpiod_set_value_cansleep(ctx->reset_gpio, 0);
-    usleep_range(15000, 15100);
+    gpiod_set_value_cansleep(pinfo->reset_gpio, 1);
+    // gpiod_set_value_cansleep(pinfo->mcu_reset_gpio, 1);
+    usleep_range(10000, 10100);
+// }
+
+// Firmware loading infrastructure start.
+// 
+// /*static int check_fw_checksum_write_size(const struct firmware *fw,struct chip_data_nt36523 *chip_info)
+//  {
+//  // check FW need to write size
+//  if (nvt_get_fw_need_write_size(fw, chip_info)) {
+//      TPD_INFO("get fw need to write size fail!\n");
+//      return -1;
+//  }
+// 
+//  // check if FW version add FW version bar equals 0xFF
+//  if (*(fw->data + (chip_info->fw_need_write_size - SIZE_4KB)) +
+//      *(fw->data + (chip_info->fw_need_write_size - SIZE_4KB + 1)) !=
+//      0xFF) {
+//      TPD_INFO("bin file FW_VER + FW_VER_BAR should be 0xFF!\n");
+//  TPD_INFO("FW_VER=0x%02X, FW_VER_BAR=0x%02X\n",
+//           *(fw->data + (chip_info->fw_need_write_size - SIZE_4KB)),
+//           *(fw->data + (chip_info->fw_need_write_size - SIZE_4KB + 1)));
+//  return -1;
+//      }
+//      return 0;
+//      }
+// */
+// /*static int32_t nvt_check_bin_checksum(const u8 *fwdata, size_t fwsize)
+// {
+//         uint32_t checksum_calculated = 0;
+//         uint32_t checksum_bin = 0;
+//         int32_t ret = 0;
+// 
+//         // calculate the checksum result
+//         checksum_calculated = CheckSum(fwdata, fwsize - FW_BIN_CHECKSUM_LEN - 1);
+//         // get the checksum from file directly
+//         checksum_bin = byte_to_word(fwdata + (fwsize - FW_BIN_CHECKSUM_LEN));
+//         if (checksum_calculated != checksum_bin) {
+//                 TPD_INFO("%s checksum_calculated = 0x%08X\n", __func__, checksum_calculated);
+//                 TPD_INFO("%s checksum_bin = 0x%08X\n", __func__, checksum_bin);
+//                 ret = -EINVAL;
+//         }
+// 
+//         return ret;
+// }*/
+// static unsigned int CheckSum(const u8* data, size_t len)
+// {
+//     uint32_t i = 0;
+//     uint32_t checksum = 0;
+// 
+//     for (i = 0; i < len; i++) {
+//         checksum += data[i];
+//     }
+// 
+//     checksum += (len - 1);
+//     checksum = ~checksum + 1;
+// 
+//     return checksum;
+// }
+// 
+// static int novatek_fw_check_checksum(struct drm_panel* panel, const struct firmware* fw)
+// {
+//     uint32_t checksum_calculated = 0;
+//     uint32_t checksum_bin = 0;
+//     size_t payload_size;
+// 
+//     if (fw->size <= FW_BIN_CHECKSUM_LEN)
+//         return -EINVAL;
+// 
+//     payload_size = fw->size - FW_BIN_CHECKSUM_LEN;
+// 
+//     /* Get the expected checksum embedded at the very end of the file */
+//     checksum_bin = le32_to_cpup((__le32*)&fw->data[payload_size]);
+// 
+//     checksum_calculated = CheckSum(fw->data, payload_size);
+// 
+//     if (checksum_calculated != checksum_bin) {
+//         dev_err(panel->dev, "Checksum mismatch! Calc: 0x%08X, Bin: 0x%08X\n",
+//             checksum_calculated, checksum_bin);
+//         return -EINVAL;
+//     }
+// 
+//     return 0;
+// }
+// 
+// /*static int32_t nvt_bin_header_parser(struct chip_data_nt36523 *chip_info,
+//                                          const u8 *fwdata, size_t fwsize)
+// {
+//         uint32_t list = 0;
+//         uint32_t pos = 0x00;
+//         uint32_t tmp_end = 0x00;
+//         uint8_t info_sec_num = 0;
+//         uint8_t ovly_sec_num = 0;
+//         uint8_t ovly_info = 0;
+//         uint8_t find_bin_header = 0;
+// 
+//         // Find the header size
+//         tmp_end = fwdata[0] + (fwdata[1] << 8) + (fwdata[2] << 16) + (fwdata[3] << 24);
+// 
+//         // check cascade next header
+//         chip_info->cascade_2nd_header_info = (fwdata[0x20] & 0x02) >> 1;
+//         TPD_INFO("cascade_2nd_header_info = %d\n", chip_info->cascade_2nd_header_info);
+// 
+//         if (chip_info->cascade_2nd_header_info) {
+//                 pos = 0x30;	// info section start at 0x30 offset
+//                 while (pos < (tmp_end / 2)) {
+//                         info_sec_num++;
+//                         pos += 0x10;	// each header info is 16 bytes
+//                 }
+// 
+//                 info_sec_num = info_sec_num + 1; // next header section
+//         } else {
+//                 pos = 0x30;	// info section start at 0x30 offset
+//                 while (pos < tmp_end) {
+//                         info_sec_num++;
+//                         pos += 0x10;	// each header info is 16 bytes
+//                 }
+//         }
+// 
+// 
+//         // * Find the DLM OVLY section
+//         // * [0:3] Overlay Section Number
+//         // * [4]   Overlay Info
+// 
+//         ovly_info = (fwdata[0x28] & 0x10) >> 4;
+//         ovly_sec_num = (ovly_info) ? (fwdata[0x28] & 0x0F) : 0;
+// 
+// 
+//         // * calculate all partition number
+//         // * ilm_dlm_num (ILM & DLM) + ovly_sec_num + info_sec_num
+// 
+//         chip_info->partition = chip_info->ilm_dlm_num + ovly_sec_num + info_sec_num;
+//         TPD_DEBUG("ovly_info = %d, ilm_dlm_num = %d, ovly_sec_num = %d, info_sec_num = %d, partition = %d\n",
+//                  ovly_info, chip_info->ilm_dlm_num, ovly_sec_num, info_sec_num,
+//                  chip_info->partition);
+// 
+//         // allocated memory for header info
+//         chip_info->bin_map = (struct nvt_ts_bin_map *)tp_devm_kzalloc(
+//                                          &chip_info->s_client->dev,
+//                                          (chip_info->partition + 1) * sizeof(struct nvt_ts_bin_map), GFP_KERNEL);
+// 
+//         if(chip_info->bin_map == NULL) {
+//         TPD_INFO("kzalloc for bin_map failed!\n");
+//         return -ENOMEM;
+//         }
+// 
+//         for (list = 0; list < chip_info->partition; list++) {
+// 
+//                 // * [1] parsing ILM & DLM header info
+//                 // * BIN_addr : SRAM_addr : size (12-bytes)
+//                 // * crc located at 0x18 & 0x1C
+// 
+//                 if (list < chip_info->ilm_dlm_num) {
+//                         chip_info->bin_map[list].BIN_addr = byte_to_word(&fwdata[0 + list * 12]);
+//                         chip_info->bin_map[list].SRAM_addr = byte_to_word(&fwdata[4 + list * 12]);
+//                         chip_info->bin_map[list].size = byte_to_word(&fwdata[8 + list * 12]);
+//                         chip_info->bin_map[list].crc = byte_to_word(&fwdata[0x18 + list * 4]);
+//                         if (list == 0) {
+//                                 snprintf(chip_info->bin_map[list].name, 12, "ILM");
+// 
+//                         } else if (list == 1) {
+//                                 snprintf(chip_info->bin_map[list].name, 12, "DLM");
+//                         }
+//                 }
+// 
+// 
+//                 // * [2] parsing others header info
+//                 // * SRAM_addr : size : BIN_addr : crc (16-bytes)
+// 
+//                 if ((list >= chip_info->ilm_dlm_num)
+//                                 && (list < (chip_info->ilm_dlm_num + info_sec_num))) {
+//                         if (find_bin_header == 0) {
+//                         // others partition located at 0x30 offset
+//                         pos = 0x30 + (0x10 * (list - chip_info->ilm_dlm_num));
+//                         } else if (find_bin_header && chip_info->cascade_2nd_header_info) {
+//                                 // cascade 2nd header info
+//                                 pos = tmp_end - 0x10;
+//                         }
+// 
+//                         chip_info->bin_map[list].SRAM_addr = byte_to_word(&fwdata[pos]);
+//                         chip_info->bin_map[list].size = byte_to_word(&fwdata[pos + 4]);
+//                         chip_info->bin_map[list].BIN_addr = byte_to_word(&fwdata[pos + 8]);
+//                         chip_info->bin_map[list].crc = byte_to_word(&fwdata[pos + 12]);
+//                         // detect header end to protect parser function
+//                         if ((chip_info->bin_map[list].BIN_addr < tmp_end) && (chip_info->bin_map[list].size != 0)) {
+//                                 snprintf(chip_info->bin_map[list].name, 12, "Header");
+//                                 find_bin_header = 1;
+//                         } else {
+//                                 snprintf(chip_info->bin_map[list].name, 12, "Info-%d",
+//                                         (list - chip_info->ilm_dlm_num));
+//                         }
+//                 }
+// 
+// 
+//                 // * [3] parsing overlay section header info
+//                 // * SRAM_addr : size : BIN_addr : crc (16-bytes)
+// 
+//                 if (list >= (chip_info->ilm_dlm_num + info_sec_num)) {
+//                         // overlay info located at DLM (list = 1) start addr
+//                         pos = chip_info->bin_map[1].BIN_addr + (0x10 * (list - chip_info->ilm_dlm_num -
+//                                                                 info_sec_num));
+// 
+//                         chip_info->bin_map[list].SRAM_addr = byte_to_word(&fwdata[pos]);
+//                         chip_info->bin_map[list].size = byte_to_word(&fwdata[pos + 4]);
+//                         chip_info->bin_map[list].BIN_addr = byte_to_word(&fwdata[pos + 8]);
+//                         chip_info->bin_map[list].crc = byte_to_word(&fwdata[pos + 12]);
+//                         snprintf(chip_info->bin_map[list].name, 12, "Overlay-%d",
+//                                 (list - chip_info->ilm_dlm_num - info_sec_num));
+//                 }
+// 
+//                 // BIN size error detect
+//                 if ((chip_info->bin_map[list].BIN_addr + chip_info->bin_map[list].size) >
+//                                 fwsize) {
+//                         TPD_INFO("access range (0x%08X to 0x%08X) is larger than bin size!\n",
+//                                 chip_info->bin_map[list].BIN_addr,
+//                                 chip_info->bin_map[list].BIN_addr + chip_info->bin_map[list].size);
+//                         return -EINVAL;
+//                 }
+//         }
+// 
+//         return 0;
+// }*/
+// static int novatek_fw_parse_header(struct drm_panel* panel, const struct firmware* fw,
+//     struct fw_header* fw_data)
+// {
+//     // const u8* data = fw->data;
+//     //
+//     // /* The header must be at least large enough to contain the ILM/DLM offsets (0x30 bytes) */
+//     // if (fw->size < 0x30) {
+//     //     dev_err(panel->dev, "Firmware file too small to contain headers\n");
+//     //     return -EINVAL;
+//     // }
+//     //
+//     // /*
+//     //  * Extract ILM (Instruction RAM) Partition Info
+//     //  * Using get_unaligned_le32 to safely read 32-bit integers from a packed byte array
+//     //  */
+//     // fw_data->ilm.bin_addr = get_unaligned_le32(&data[0x00]);
+//     // fw_data->ilm.sram_addr = get_unaligned_le32(&data[0x04]);
+//     // fw_data->ilm.size = get_unaligned_le32(&data[0x08]);
+//     // fw_data->ilm.crc = get_unaligned_le32(&data[0x18]);
+//     //
+//     // /* Extract DLM (Data RAM) Partition Info */
+//     // fw_data->dlm.bin_addr = get_unaligned_le32(&data[0x0C]);
+//     // fw_data->dlm.sram_addr = get_unaligned_le32(&data[0x10]);
+//     // fw_data->dlm.size = get_unaligned_le32(&data[0x14]);
+//     // fw_data->dlm.crc = get_unaligned_le32(&data[0x1C]);
+//     //
+//     // /* Extract Cascade Flag */
+//     // fw_data->is_cascade = (data[0x20] & 0x02) >> 1;
+//     //
+//     // dev_dbg(panel->dev, "ILM: SRAM 0x%X, Size 0x%X. DLM: SRAM 0x%X, Size 0x%X. Cascade: %d\n",
+//     //     fw_data->ilm.sram_addr, fw_data->ilm.size,
+//     //     fw_data->dlm.sram_addr, fw_data->dlm.size, fw_data->is_cascade);
+//     //
+//     // /* Boundary Safety Check */
+//     // if (fw_data->ilm.bin_addr + fw_data->ilm.size > fw->size || fw_data->dlm.bin_addr + fw_data->dlm.size > fw->size) {
+//     //     dev_err(panel->dev, "Firmware partitions exceed file size bounds!\n");
+//     //     return -EINVAL;
+//     // }
+//     //
+//     // return 0;
+//     const u8* data = fw->data;
+//     uint32_t tmp_end, pos = 0x30;
+//     uint8_t info_sec_num = 0, ovly_sec_num = 0, ovly_info;
+//     uint8_t find_bin_header = 0;
+//     uint32_t list = 0, i;
+// 
+//     if (fw->size < 0x30)
+//         return -EINVAL;
+// 
+//     tmp_end = get_unaligned_le32(&data[0]);
+//     fw_data->is_cascade = (data[0x20] & 0x02) >> 1;
+// 
+//     if (fw_data->is_cascade) {
+//         while (pos < (tmp_end / 2)) {
+//             info_sec_num++;
+//             pos += 0x10;
+//         }
+//         info_sec_num++;
+//     } else {
+//         while (pos < tmp_end) {
+//             info_sec_num++;
+//             pos += 0x10;
+//         }
+//     }
+// 
+//     ovly_info = (data[0x28] & 0x10) >> 4;
+//     ovly_sec_num = ovly_info ? (data[0x28] & 0x0F) : 0;
+// 
+//     fw_data->num_partitions = 2 + ovly_sec_num + info_sec_num;
+//     if (fw_data->num_partitions > MAX_PARTITIONS)
+//         return -EINVAL;
+// 
+//     /* Parse ILM (0) & DLM (1) */
+//     for (list = 0; list < 2; list++) {
+//         fw_data->parts[list].bin_addr = get_unaligned_le32(&data[0 + list * 12]);
+//         fw_data->parts[list].sram_addr = get_unaligned_le32(&data[4 + list * 12]);
+//         fw_data->parts[list].size = get_unaligned_le32(&data[8 + list * 12]);
+//         fw_data->parts[list].crc = get_unaligned_le32(&data[0x18 + list * 4]);
+//     }
+// 
+//     /* Parse Info Sections (with the cascade jump hack!) */
+//     for (i = 0; i < info_sec_num; i++, list++) {
+//         if (find_bin_header == 0) {
+//             pos = 0x30 + (0x10 * i);
+//         } else if (find_bin_header && fw_data->is_cascade) {
+//             pos = tmp_end - 0x10; /* Jump to the end of the header block */
+//         }
+// 
+//         fw_data->parts[list].sram_addr = get_unaligned_le32(&data[pos]);
+//         fw_data->parts[list].size = get_unaligned_le32(&data[pos + 4]);
+//         fw_data->parts[list].bin_addr = get_unaligned_le32(&data[pos + 8]);
+//         fw_data->parts[list].crc = get_unaligned_le32(&data[pos + 12]);
+// 
+//         /* Flag that we found the final bin boundary so the next loop jumps */
+//         if ((fw_data->parts[list].bin_addr < tmp_end) && (fw_data->parts[list].size != 0)) {
+//             find_bin_header = 1;
+//         }
+//     }
+// 
+//     /* Parse DLM Overlay Sections */
+//     for (i = 0; i < ovly_sec_num; i++, list++) {
+//         pos = fw_data->parts[1].bin_addr + (0x10 * i);
+//         fw_data->parts[list].sram_addr = get_unaligned_le32(&data[pos]);
+//         fw_data->parts[list].size = get_unaligned_le32(&data[pos + 4]);
+//         fw_data->parts[list].bin_addr = get_unaligned_le32(&data[pos + 8]);
+//         fw_data->parts[list].crc = get_unaligned_le32(&data[pos + 12]);
+//     }
+// 
+//     return 0;
 }
 
+// /*static fw_check_state nvt_fw_check(void *chip_data,
+//                                    struct resolution_info *resolution_info, struct panel_info *panel_data)
+// {
+//         uint8_t ver_len = 0;
+//         uint8_t fw_ver_len = 0;
+//         int ret = 0;
+//         char dev_version[MAX_DEVICE_VERSION_LENGTH] = {0};
+//         struct chip_data_nt36523 *chip_info = (struct chip_data_nt36523 *)chip_data;
+// 
+//         nvt_esd_check_update_timer(chip_info);
+// 
+//         ret |= nvt_get_fw_info_noflash(chip_info);
+// 
+//         if (ret < 0) {
+//                 TPD_INFO("%s: get fw info failed\n", __func__);
+//                 return FW_ABNORMAL;
+// 
+//         } else {
+//                 panel_data->tp_fw = chip_info->fw_ver;
+//                 snprintf(dev_version, MAX_DEVICE_VERSION_LENGTH,
+//                          "%02X", panel_data->tp_fw);
+//                 TPD_INFO("%s: dev_version = %s\n", __func__, dev_version);
+// 
+//                 if (panel_data->manufacture_info.version) {
+//                         ver_len = strlen(panel_data->manufacture_info.version);
+//                         fw_ver_len = strlen(dev_version);
+//                         snprintf(panel_data->manufacture_info.version + (ver_len - fw_ver_len),
+//                                  sizeof(dev_version),
+//                                  dev_version);
+//                         TPD_DEBUG("%s: firmware_version = %s\n", __func__, panel_data->manufacture_info.version);
+//                 }
+//         }
+// 
+//         return FW_NORMAL;
+// }*/
+// 
+// /*static void novatek_esd_check_update_timer()
+// {
+//     // update interrupt timer
+//     chip_info->irq_timer = jiffies;
+// 
+//     // Do I even need it............
+//     // Seems that I'm free to drop it.
+// }*/
+// 
+// /*int32_t CTP_SPI_WRITE(struct spi_device* client, uint8_t* buf, uint16_t len)
+// {
+//     int32_t ret = -1;
+//     int32_t retries = 0;
+// 
+//     buf[0] = SPI_WRITE_MASK(buf[0]);
+// 
+//     while (retries < 5) {
+//         ret = spi_read_write(client, buf, len, NULL, SPIWRITE);
+// 
+//         if (ret == 0) {
+//             break;
+//         }
+// 
+//         retries++;
+//     }
+// 
+//     if (unlikely(retries == 5)) {
+//         TPD_INFO("error, ret=%d\n", ret);
+//         ret = -EIO;
+//     }
+// 
+//     return ret;
+// }*/
+// static int nvt_spi_write_cmd(struct spi_device* spi, u8* cmd, size_t len)
+// {
+//     u8* dma_buf;
+//     int ret;
+// 
+//     dma_buf = kmemdup(cmd, len, GFP_KERNEL);
+//     if (!dma_buf)
+//         return -ENOMEM;
+// 
+//     /* Vendor masking: Highest bit must be 0 for Write */
+//     dma_buf[0] |= 0x80;
+// 
+//     ret = spi_write(spi, dma_buf, len);
+//     kfree(dma_buf);
+//     return ret;
+// }
+// 
+// /*static int32_t nvt_set_page(struct chip_data_nt36523 *chip_info, uint32_t addr)
+// {
+//     uint8_t buf[4] = {0};
+// 
+//     buf[0] = 0xFF;	  // set index/page/addr command
+//     buf[1] = (addr >> 15) & 0xFF;
+//     buf[2] = (addr >> 7) & 0xFF;
+// 
+//     return CTP_SPI_WRITE(chip_info->s_client, buf, 3);
+// }*/
+// static int nvt_set_page(struct spi_device* spi, uint32_t addr)
+// {
+//     u8 buf[3] = {
+//         0xFF, /* set index/page/addr command */
+//         (addr >> 15) & 0xFF,
+//         (addr >> 7) & 0xFF
+//     };
+//     return nvt_spi_write_cmd(spi, buf, 3);
+// }
+// 
+// /*int32_t CTP_SPI_READ(struct spi_device* client, uint8_t* buf, uint16_t len)
+// {
+//     int32_t ret = -1;
+//     int32_t retries = 0;
+//     uint8_t rbuf[SPI_TANSFER_LEN + 1] = { 0 };
+// 
+//     buf[0] = SPI_READ_MASK(buf[0]);
+// 
+//     while (retries < 5) {
+//         ret = spi_read_write(client, buf, len, rbuf, SPIREAD);
+// 
+//         if (ret == 0) {
+//             break;
+//         }
+// 
+//         retries++;
+//     }
+// 
+//     if (unlikely(retries == 5)) {
+//         TPD_INFO("read error, ret=%d\n", ret);
+//         ret = -EIO;
+// 
+//     } else {
+//         memcpy((buf + 1), (rbuf + 2), (len - 1));
+//     }
+// 
+//     return ret;
+// }*/
+// static int nvt_spi_read_cmd(struct spi_device* spi, u8 reg_offset, u8* val)
+// {
+//     u8* dma_buf;
+//     int ret;
+//     struct spi_transfer t = { 0 };
+//     struct spi_message m;
+// 
+//     dma_buf = kzalloc(3, GFP_KERNEL);
+//     if (!dma_buf)
+//         return -ENOMEM;
+// 
+//     // READ - & 0x7F, WRITE - | 0x80
+//     dma_buf[0] = reg_offset & 0x7F;
+//     dma_buf[1] = 0xFF; /* Dummy byte to clock the MISO line */
+// 
+//     t.tx_buf = dma_buf;
+//     t.rx_buf = dma_buf;
+//     t.len = 3;
+// 
+//     spi_message_init(&m);
+//     spi_message_add_tail(&t, &m);
+// 
+//     ret = spi_sync(spi, &m);
+//     if (!ret)
+//         *val = dma_buf[2];
+// 
+//     kfree(dma_buf);
+//     return ret;
+// }
+// 
+// /* Mainline helper for multi-byte SPI reads */
+// static int nvt_spi_read_block(struct spi_device* spi, unsigned int reg_offset, u8* data, size_t len)
+// {
+//     u8* dma_buf;
+//     int ret;
+//     struct spi_transfer t = { 0 };
+//     struct spi_message m;
+// 
+//     /* Total transfer size: 1 CMD + 1 DUMMY + len DATA */
+//     dma_buf = kzalloc(2 + len, GFP_KERNEL);
+//     if (!dma_buf)
+//         return -ENOMEM;
+// 
+//     /* Set Read Mask (MSB = 0) and Dummy Byte */
+//     dma_buf[0] = reg_offset & 0x7F;
+//     dma_buf[1] = 0xFF;
+// 
+//     t.tx_buf = dma_buf;
+//     t.rx_buf = dma_buf;
+//     t.len = 2 + len;
+// 
+//     spi_message_init(&m);
+//     spi_message_add_tail(&t, &m);
+// 
+//     ret = spi_sync(spi, &m);
+//     if (!ret)
+//         memcpy(data, &dma_buf[2], len); /* Copy the data payload out */
+// 
+//     kfree(dma_buf);
+//     return ret;
+// }
+// 
+// /*static int32_t nvt_write_addr(struct spi_device *client, uint32_t addr,
+//                               uint8_t data)
+// {
+//     int32_t ret = 0;
+//     uint8_t buf[4] = {0};
+// 
+//     //---set xdata index---
+//     buf[0] = 0xFF;  // set index/page/addr command
+//     buf[1] = (addr >> 15) & 0xFF;
+//     buf[2] = (addr >> 7) & 0xFF;
+//     ret = CTP_SPI_WRITE(client, buf, 3);
+//     if (ret) {
+//         TPD_INFO("set page 0x%06X failed, ret = %d\n", addr, ret);
+//         return ret;
+//     }
+// 
+//     // ---write data to index---
+//     buf[0] = addr & (0x7F);
+//     buf[1] = data;
+//     ret = CTP_SPI_WRITE(client, buf, 2);
+//     if (ret) {
+//         TPD_INFO("write data to 0x%06X failed, ret = %d\n", addr, ret);
+//         return ret;
+//     }
+// 
+//     return ret;
+// }*/
+// static int nvt_write_addr(struct spi_device* spi, uint32_t addr, uint8_t data)
+// {
+//     u8 buf[2] = {
+//         addr & 0x7F,
+//         data
+//     };
+//     int ret;
+// 
+//     ret = nvt_set_page(spi, addr);
+//     if (ret)
+//         return ret;
+// 
+//     return nvt_spi_write_cmd(spi, buf, 2);
+// }
+// 
+// /*static void novatek_bootloader_reset(struct spi_device* spi)
+// {
+//     nvt_write_addr(spi, chip_info->swrst_sif_addr, 0x69);
+// 
+//     mdelay(5); // wait tBRST2FR after Bootload RST
+// }
+// */
+// static int novatek_bootloader_reset(struct spi_device* spi)
+// {
+//     int ret;
+// 
+//     /* Halt MCU and wipe pointers */
+//     ret = nvt_write_addr(spi, NVT_SWRST_SIF_ADDR, 0x69);
+//     if (ret) {
+//         dev_err(&spi->dev, "Failed to send bootloader reset command: %d\n", ret);
+//         return ret;
+//     }
+// 
+//     mdelay(5); /* Wait tBRST2FR after Bootload RST */
+// 
+//     return 0;
+// }
+// 
+// /*static void nvt_set_bld_crc_bank(struct spi_device* spi,
+//     uint32_t DES_ADDR, uint32_t SRAM_ADDR,
+//     uint32_t LENGTH_ADDR, uint32_t size,
+//     uint32_t G_CHECKSUM_ADDR, uint32_t crc)
+// {
+//     // write destination address
+//     nvt_set_page(spi, DES_ADDR);
+//     chip_info->fwbuf[0] = DES_ADDR & 0x7F;
+//     chip_info->fwbuf[1] = (SRAM_ADDR) & 0xFF;
+//     chip_info->fwbuf[2] = (SRAM_ADDR >> 8) & 0xFF;
+//     chip_info->fwbuf[3] = (SRAM_ADDR >> 16) & 0xFF;
+//     CTP_SPI_WRITE(chip_info->s_client, chip_info->fwbuf, 4);
+// 
+//     // write length
+//     chip_info->fwbuf[0] = LENGTH_ADDR & 0x7F;
+//     chip_info->fwbuf[1] = (size) & 0xFF;
+//     chip_info->fwbuf[2] = (size >> 8) & 0xFF;
+//     chip_info->fwbuf[3] = (size >> 16) & 0xFF;
+//     if (chip_info->hw_crc == HWCRC_LEN_2Bytes) {
+//         CTP_SPI_WRITE(chip_info->s_client, chip_info->fwbuf, 3);
+//     } else if (chip_info->hw_crc >= HWCRC_LEN_3Bytes) {
+//         CTP_SPI_WRITE(chip_info->s_client, chip_info->fwbuf, 4);
+//     }
+// 
+//     // write golden dlm checksum
+//     chip_info->fwbuf[0] = G_CHECKSUM_ADDR & 0x7F;
+//     chip_info->fwbuf[1] = (crc) & 0xFF;
+//     chip_info->fwbuf[2] = (crc >> 8) & 0xFF;
+//     chip_info->fwbuf[3] = (crc >> 16) & 0xFF;
+//     chip_info->fwbuf[4] = (crc >> 24) & 0xFF;
+//     CTP_SPI_WRITE(chip_info->s_client, chip_info->fwbuf, 5);
+// 
+//     return;
+// }*/
+// static int nvt_set_bld_crc_bank(struct spi_device* spi,
+//     uint32_t des_addr, uint32_t sram_addr,
+//     uint32_t length_addr, uint32_t size,
+//     uint32_t checksum_addr, uint32_t crc)
+// {
+//     u8 buf[5];
+//     int ret;
+// 
+//     /* 1. Write Destination Address (Offset + 3 bytes SRAM Addr) */
+//     ret = nvt_set_page(spi, des_addr);
+//     if (ret)
+//         return ret;
+// 
+//     buf[0] = des_addr & 0x7F;
+//     buf[1] = sram_addr & 0xFF;
+//     buf[2] = (sram_addr >> 8) & 0xFF;
+//     buf[3] = (sram_addr >> 16) & 0xFF;
+//     ret = nvt_spi_write_cmd(spi, buf, 4);
+//     if (ret)
+//         return ret;
+// 
+//     /* 2. Write Length (Offset + 3 bytes Size)
+//      * Modern Novatek ICs (like NT36532) use 3-byte hardware CRC lengths.
+//      */
+//     buf[0] = length_addr & 0x7F;
+//     buf[1] = size & 0xFF;
+//     buf[2] = (size >> 8) & 0xFF;
+//     buf[3] = (size >> 16) & 0xFF;
+//     ret = nvt_spi_write_cmd(spi, buf, 4);
+//     if (ret)
+//         return ret;
+// 
+//     /* 3. Write Golden Checksum (Offset + 4 bytes CRC) */
+//     buf[0] = checksum_addr & 0x7F;
+//     buf[1] = crc & 0xFF;
+//     buf[2] = (crc >> 8) & 0xFF;
+//     buf[3] = (crc >> 16) & 0xFF;
+//     buf[4] = (crc >> 24) & 0xFF;
+//     ret = nvt_spi_write_cmd(spi, buf, 5);
+// 
+//     return ret;
+// }
+// 
+// /*static void novatek_set_bld_hw_crc()
+// {
+//     // [0] ILM
+//     // write register bank
+//     nvt_set_bld_crc_bank(chip_info,
+//         ILM_DES_ADDR, chip_info->bin_map[0].SRAM_addr,
+//         ILM_LENGTH_ADDR, chip_info->bin_map[0].size,
+//         G_ILM_CHECKSUM_ADDR, chip_info->bin_map[0].crc);
+// 
+//     // [1] DLM
+//     // write register bank
+//     nvt_set_bld_crc_bank(chip_info,
+//         DLM_DES_ADDR, chip_info->bin_map[1].SRAM_addr,
+//         DLM_LENGTH_ADDR, chip_info->bin_map[1].size,
+//         G_DLM_CHECKSUM_ADDR, chip_info->bin_map[1].crc);
+// }*/
+// static int novatek_set_bld_hw_crc(struct spi_device* spi, struct fw_header* fw_data)
+// {
+//     int ret;
+// 
+//     ret = nvt_set_bld_crc_bank(spi, NVT_ILM_DES_ADDR, fw_data->parts[0].sram_addr,
+//         NVT_ILM_LENGTH_ADDR, fw_data->parts[0].size,
+//         NVT_G_ILM_CHECKSUM_ADDR, fw_data->parts[0].crc);
+//     if (ret)
+//         return ret;
+// 
+//     return nvt_set_bld_crc_bank(spi, NVT_DLM_DES_ADDR, fw_data->parts[1].sram_addr,
+//         NVT_DLM_LENGTH_ADDR, fw_data->parts[1].size,
+//         NVT_G_DLM_CHECKSUM_ADDR, fw_data->parts[1].crc);
+// }
+// 
+// /*static void novatek_tx_auto_copy_mode()
+// {
+//     if (chip_info->auto_copy == CHECK_SPI_DMA_TX_INFO) {
+//         // ---write TX_AUTO_COPY_EN cmds---
+//         nvt_write_addr(chip_info->s_client, chip_info->trim_id_table.mmap->TX_AUTO_COPY_EN, 0x69);
+//     } else if (chip_info->auto_copy == CHECK_TX_AUTO_COPY_EN) {
+//         // ---write SPI_MST_AUTO_COPY cmds---
+//         nvt_write_addr(chip_info->s_client, chip_info->trim_id_table.mmap->TX_AUTO_COPY_EN, 0x56);
+//     }
+// 
+//     printf("auto copy mode enabled\n");
+// }*/
+// static int novatek_tx_auto_copy_mode(struct spi_device* spi, u8 copy_cmd)
+// {
+//     int ret;
+// 
+//     /*
+//      * copy_cmd is either 0x69 (CHECK_SPI_DMA_TX_INFO)
+//      * or 0x56 (CHECK_TX_AUTO_COPY_EN).
+//      */
+//     ret = nvt_write_addr(spi, NVT_TX_AUTO_COPY_EN, copy_cmd);
+//     if (ret) {
+//         dev_err(&spi->dev, "Failed to enable auto copy mode: %d\n", ret);
+//         return ret;
+//     }
+// 
+//     dev_dbg(&spi->dev, "Auto copy mode enabled (cmd: 0x%02x)\n", copy_cmd);
+//     return 0;
+// }
+// 
+// /*static int32_t nvt_check_spi_dma_tx_info(struct chip_data_nt36523* chip_info)
+// {
+//     uint8_t buf[8] = { 0 };
+//     int32_t i = 0;
+//     const int32_t retry = 200;
+// 
+//     if (chip_info->trim_id_table.mmap->SPI_DMA_TX_INFO == 0) {
+//         TPD_INFO("error, SPI_DMA_TX_INFO = 0\n");
+//         return -1;
+//     }
+// 
+//     for (i = 0; i < retry; i++) {
+//         // ---set xdata index to SPI_DMA_TX_INFO---
+//         nvt_set_page(chip_info, chip_info->trim_id_table.mmap->SPI_DMA_TX_INFO);
+// 
+//         // ---read fw status---
+//         buf[0] = chip_info->trim_id_table.mmap->SPI_DMA_TX_INFO & 0x7F;
+//         buf[1] = 0xFF;
+//         CTP_SPI_READ(chip_info->s_client, buf, 2);
+// 
+//         if (buf[1] == 0x00)
+//             break;
+// 
+//         usleep_range(1000, 1000);
+//     }
+// 
+//     if (i >= retry) {
+//         TPD_INFO("failed, i=%d, buf[1]=0x%02X\n", i, buf[1]);
+//         return -1;
+//     } else {
+//         return 0;
+//     }
+// }*/
+// /*static int nvt_check_spi_dma_tx_info(struct spi_device* spi)
+// {
+//     int ret, i;
+//     u8 val;
+//     const int retry = 200;
+// 
+//     for (i = 0; i < retry; i++) {
+//         // Set the memory pager to the status register
+//         ret = nvt_set_page(spi, NVT_SPI_DMA_TX_INFO);
+//         if (ret)
+//             return ret;
+// 
+//         // Read the status byte
+//         ret = nvt_spi_read_cmd(spi, NVT_SPI_DMA_TX_INFO & 0x7F, &val);
+//         if (ret)
+//             return ret;
+// 
+//         // 0x00 means the DMA controller finished its task
+//         if (val == 0x00)
+//             return 0;
+// 
+//         usleep_range(1000, 1500); // Vendor used 1000, adding wiggle room for mainline
+//     }
+// 
+//     dev_err(&spi->dev, "SPI DMA TX info check timed out! Last val = 0x%02X\n", val);
+//     return -ETIMEDOUT;
+// }*/
+// 
+// /*static int32_t nvt_check_tx_auto_copy(struct chip_data_nt36523* chip_info)
+// {
+//     uint8_t buf[8] = { 0 };
+//     int32_t i = 0;
+//     const int32_t retry = 200;
+// 
+//     if (chip_info->trim_id_table.mmap->TX_AUTO_COPY_EN == 0) {
+//         TPD_INFO("error, TX_AUTO_COPY_EN = 0\n");
+//         return -1;
+//     }
+// 
+//     for (i = 0; i < retry; i++) {
+//         // ---set xdata index to SPI_MST_AUTO_COPY---
+//         nvt_set_page(chip_info, chip_info->trim_id_table.mmap->TX_AUTO_COPY_EN);
+// 
+//         // ---read auto copy status---
+//         buf[0] = chip_info->trim_id_table.mmap->TX_AUTO_COPY_EN & 0x7F;
+//         buf[1] = 0xFF;
+//         CTP_SPI_READ(chip_info->s_client, buf, 2);
+// 
+//         if (buf[1] == 0x00)
+//             break;
+// 
+//         usleep_range(1000, 1000);
+//     }
+// 
+//     if (i >= retry) {
+//         TPD_INFO("failed, i=%d, buf[1]=0x%02X\n", i, buf[1]);
+//         return -1;
+//     } else {
+//         return 0;
+//     }
+// }*/
+// /* static int novatek_wait_auto_copy()
+// {
+//     if (chip_info->auto_copy == CHECK_SPI_DMA_TX_INFO) {
+//         return nvt_check_spi_dma_tx_info(chip_info);
+//     } else if (chip_info->auto_copy == CHECK_TX_AUTO_COPY_EN) {
+//         return nvt_check_tx_auto_copy(chip_info);
+//     } else {
+//         TPD_INFO("failed, not support mode %d!\n", chip_info->auto_copy);
+//         return -1;
+//     }
+// }*/
+// static int novatek_wait_auto_copy(struct spi_device* spi)
+// {
+//     int ret, i;
+//     u8 val;
+// 
+//     for (i = 0; i < 200; i++) {
+//         ret = nvt_set_page(spi, NVT_TX_AUTO_COPY_EN);
+//         if (ret)
+//             return ret;
+// 
+//         ret = nvt_spi_read_block(spi, NVT_TX_AUTO_COPY_EN & 0x7F, &val, 1);
+//         if (!ret && val == 0x00)
+//             return 0;
+// 
+//         usleep_range(1000, 1500);
+//     }
+// 
+//     return -ETIMEDOUT;
+// }
+// 
+// /*static void novatek_stop_crc_reboot()
+// {
+//     uint8_t buf[8] = { 0 };
+//     int32_t retry = 0;
+// 
+//     // IC is in CRC fail reboot loop, needs to be stopped!
+//     for (retry = 5; retry > 0; retry--) {
+//         // ---reset idle : 1st---
+//         nvt_write_addr(chip_info->s_client, chip_info->swrst_sif_addr, 0xAA);
+// 
+//         // ---reset idle : 2rd---
+//         nvt_write_addr(chip_info->s_client, chip_info->swrst_sif_addr, 0xAA);
+// 
+//         msleep(1);
+// 
+//         // ---clear CRC_ERR_FLAG---
+//         nvt_set_page(chip_info, chip_info->crc_err_flag_addr);
+// 
+//         buf[0] = chip_info->crc_err_flag_addr & 0xFF;
+//         buf[1] = 0xA5;
+//         CTP_SPI_WRITE(chip_info->s_client, buf, 2);
+// 
+//         // ---check CRC_ERR_FLAG---
+//         nvt_set_page(chip_info, chip_info->crc_err_flag_addr);
+// 
+//         buf[0] = chip_info->crc_err_flag_addr & 0xFF;
+//         buf[1] = 0x00;
+//         CTP_SPI_READ(chip_info->s_client, buf, 2);
+// 
+//         if (buf[1] == 0xA5) {
+//             nvt_esd_reverse_phase(chip_info);
+//             break;
+//         }
+//     }
+// 
+//     if (retry == 0)
+//         TPD_INFO("CRC auto reboot is not able to be stopped! buf[1]=0x%02X\n", buf[1]);
+// }*/
+// static void novatek_stop_crc_reboot(struct spi_device* spi)
+// {
+//     int i;
+//     u8 val;
+// 
+//     /* IC is in CRC fail reboot loop, needs to be stopped! */
+//     for (i = 0; i < 5; i++) {
+//         /* Reset idle 1st & 2nd */
+//         nvt_write_addr(spi, NVT_SWRST_SIF_ADDR, 0xAA);
+//         nvt_write_addr(spi, NVT_SWRST_SIF_ADDR, 0xAA);
+//         msleep(1);
+// 
+//         /* Clear CRC_ERR_FLAG */
+//         nvt_write_addr(spi, NVT_DMA_CRC_FLAG_ADDR & 0xFF, 0xA5);
+// 
+//         /* Verify CRC_ERR_FLAG was cleared */
+//         nvt_set_page(spi, NVT_DMA_CRC_FLAG_ADDR);
+//         nvt_spi_read_cmd(spi, NVT_DMA_CRC_FLAG_ADDR & 0xFF, &val);
+// 
+//         if (val == 0xA5) {
+//             dev_dbg(&spi->dev, "CRC auto reboot stopped successfully.\n");
+//             return;
+//         }
+//     }
+// 
+//     dev_err(&spi->dev, "CRC auto reboot could not be stopped! val=0x%02X\n", val);
+// }
+// 
+// static int novatek_write_partition(struct spi_device* spi, const struct firmware* fw,
+//     struct nvt_partition* part)
+// {
+//     uint32_t sram_addr = part->sram_addr;
+//     uint32_t bin_addr = part->bin_addr;
+//     uint32_t size = part->size;
+//     uint32_t count, i;
+//     uint16_t len;
+//     u8* dma_buf;
+//     int ret = 0;
+// 
+//     if (!size)
+//         return 0; /* Ignore empty partitions */
+// 
+//     if (bin_addr + size > fw->size) {
+//         dev_err(&spi->dev, "Partition out of bounds: addr 0x%X size 0x%X\n", bin_addr, size);
+//         return -EINVAL;
+//     }
+// 
+//     size += 1; /* Vendor zero-index compensation */
+// 
+//     /* Allocate DMA buffer for 128 bytes of data + 1 byte for the offset address */
+//     dma_buf = kzalloc(NVT_TRANSFER_LEN + 1, GFP_KERNEL);
+//     if (!dma_buf)
+//         return -ENOMEM;
+// 
+//     count = (size / NVT_TRANSFER_LEN) + ((size % NVT_TRANSFER_LEN) ? 1 : 0);
+// 
+//     for (i = 0; i < count; i++) {
+//         len = (size < NVT_TRANSFER_LEN) ? size : NVT_TRANSFER_LEN;
+// 
+//         /* 1. Set the page to the current SRAM address */
+//         ret = nvt_set_page(spi, sram_addr);
+//         if (ret)
+//             break;
+// 
+//         /* 2. Format the buffer: [Offset Address] [ ... Data ... ] */
+//         /* Note: We enforce the 0x7F Write Mask on the first byte */
+//         dma_buf[0] = (sram_addr & 0x7F) | 0x80;
+//         memcpy(dma_buf + 1, &fw->data[bin_addr], len);
+// 
+//         /* 3. Blast the chunk over SPI directly */
+//         ret = spi_write(spi, dma_buf, len + 1);
+//         if (ret) {
+//             dev_err(&spi->dev, "SPI write failed at SRAM 0x%X\n", sram_addr);
+//             break;
+//         }
+// 
+//         sram_addr += NVT_TRANSFER_LEN;
+//         bin_addr += NVT_TRANSFER_LEN;
+//         size -= NVT_TRANSFER_LEN;
+//     }
+// 
+//     kfree(dma_buf);
+//     return ret;
+// }
+// 
+// /*static int novatek_fw_write(struct firmware* fw, struct spi_device* touch_spi)
+// {
+//     uint32_t list = 0;
+//     char* name;
+//     uint32_t BIN_addr, SRAM_addr, size;
+//     uint32_t i = 0;
+//     uint16_t len = 0;
+//     int32_t count = 0;
+//     int32_t ret = 0;
+// 
+//     for (list = 0; list < chip_info->partition; list++) {
+//         // initialize variable
+//         SRAM_addr = chip_info->bin_map[list].SRAM_addr;
+//         size = chip_info->bin_map[list].size;
+//         BIN_addr = chip_info->bin_map[list].BIN_addr;
+//         name = chip_info->bin_map[list].name;
+// 
+//         // TPD_INFO("[%d][%s] SRAM (0x%08X), SIZE (0x%08X), BIN (0x%08X)\n",
+//         // list, n*ame, SRAM_addr, size, BIN_addr);
+// 
+//         // Check data size
+//         if ((BIN_addr + size) > fwsize) {
+//             TPD_INFO("access range (0x%08X to 0x%08X) is larger than bin size!\n",
+//                 BIN_addr, BIN_addr + size);
+//             ret = -1;
+//             goto out;
+//         }
+// 
+//         // ignore reserved partition (Reserved Partition size is zero)
+//         if (!size) {
+//             continue;
+// 
+//         } else {
+//             size = size + 1;
+//         }
+// 
+//         // write data to SRAM
+//         if (size % NVT_TRANSFER_LEN) {
+//             count = (size / NVT_TRANSFER_LEN) + 1;
+//         } else {
+//             count = (size / NVT_TRANSFER_LEN);
+//         }
+// 
+//         for (i = 0; i < count; i++) {
+//             len = (size < NVT_TRANSFER_LEN) ? size : NVT_TRANSFER_LEN;
+// 
+//             // ---set xdata index to start address of SRAM---
+//             nvt_set_page(chip_info, SRAM_addr);
+// 
+//             // ---write data into SRAM---
+//             chip_info->fwbuf[0] = SRAM_addr & 0x7F; // offset
+//             memcpy(chip_info->fwbuf + 1, &fwdata[BIN_addr], len); // payload
+//             CTP_SPI_WRITE(chip_info->s_client, chip_info->fwbuf, len + 1);
+// 
+//             SRAM_addr += NVT_TRANSFER_LEN;
+//             BIN_addr += NVT_TRANSFER_LEN;
+//             size -= NVT_TRANSFER_LEN;
+//         }
+//     }
+// 
+// out:
+//     return ret;
+// }*/
+// // static int novatek_fw_write(struct spi_device* spi, const struct firmware* fw,
+// //     struct fw_header* fw_data)
+// // {
+// //     int ret;
+// //
+// //     /* Write ILM (Instruction RAM) */
+// //     ret = novatek_write_partition(spi, fw, &fw_data->parts[0]);
+// //     if (ret)
+// //         return ret;
+// //
+// //     /* Write DLM (Data RAM) */
+// //     ret = novatek_write_partition(spi, fw, &fw_data->parts[1]);
+// //
+// //     return ret;
+// // }
+// 
+// // --------------
+// 
+// /*static void novatek_fw_enable_crc()
+// {
+//     uint8_t buf[4] = { 0 };
+// 
+//     // ---set xdata index to EVENT BUF ADDR---
+//     nvt_set_page(chip_info, chip_info->trim_id_table.mmap->EVENT_BUF_ADDR);
+// 
+//     // ---clear fw reset status---
+//     buf[0] = EVENT_MAP_RESET_COMPLETE & (0x7F);
+//     buf[1] = 0x00;
+//     CTP_SPI_WRITE(chip_info->s_client, buf, 2);
+// 
+//     // ---enable fw crc---
+//     buf[0] = EVENT_MAP_HOST_CMD & (0x7F);
+//     buf[1] = 0xAE; // enable fw crc command
+//     CTP_SPI_WRITE(chip_info->s_client, buf, 2);
+// }*/
+// 
+// /*static void novatek_fw_boot_ready()
+// {
+//     // ---write BOOT_RDY status cmds---
+//     nvt_write_addr(chip_info->s_client,
+//         chip_info->trim_id_table.mmap->BOOT_RDY_ADDR, 1);
+// 
+//     mdelay(5);
+// 
+//     // ---set xdata index to EVENT BUF ADDR---
+//     nvt_set_page(chip_info, chip_info->trim_id_table.mmap->EVENT_BUF_ADDR);
+// }*/
+// 
+// static int novatek_fw_enable_crc(struct spi_device* spi)
+// {
+//     int ret;
+// 
+//     /* Set memory pager to EVENT_BUF_ADDR */
+//     ret = nvt_set_page(spi, NVT_EVENT_BUF_ADDR);
+//     if (ret)
+//         return ret;
+// 
+//     /* Clear FW reset status */
+//     ret = nvt_write_addr(spi, NVT_EVENT_BUF_ADDR | EVENT_MAP_RESET_COMPLETE, 0x00);
+//     if (ret)
+//         return ret;
+// 
+//     /* Enable HW CRC Verification */
+//     return nvt_write_addr(spi, NVT_EVENT_BUF_ADDR | EVENT_MAP_HOST_CMD, 0xAE);
+// }
+// 
+// static int novatek_fw_boot_ready(struct spi_device* spi)
+// {
+//     int ret;
+// 
+//     ret = nvt_write_addr(spi, NVT_BOOT_RDY_ADDR, 0x01);
+//     if (ret)
+//         return ret;
+// 
+//     mdelay(5); /* Wait for MCU to switch execution contexts */
+// 
+//     /* Reset the memory pager back to the standard event buffer */
+//     return nvt_set_page(spi, NVT_EVENT_BUF_ADDR);
+// }
+// 
+// /*static int novatek_fw_check_reset_state()
+// {
+//     uint8_t buf[8] = { 0 };
+//     int32_t ret = 0;
+//     int32_t retry = 0;
+//     int32_t retry_max = (check_reset_state == RESET_STATE_INIT) ? 10 : 100;
+// 
+//     while (1) {
+//         msleep(10);
+// 
+//         // ---read reset state--
+//         buf[0] = EVENT_MAP_RESET_COMPLETE;
+//         buf[1] = 0x00;
+//         CTP_SPI_READ(chip_info->s_client, buf, 6);
+// 
+//         if ((buf[1] >= check_reset_state) && (buf[1] <= RESET_STATE_MAX)) {
+//             ret = 0;
+//             break;
+//         }
+// 
+//         retry++;
+//         if (unlikely(retry > retry_max)) {
+//             TPD_INFO("error, retry=%d, buf[1]=0x%02X, 0x%02X, 0x%02X, 0x%02X, 0x%02X\n",
+//                 retry, buf[1], buf[2], buf[3], buf[4], buf[5]);
+//             ret = -1;
+//             break;
+//         }
+//     }
+// 
+//     return ret;
+// }*/
+// 
+// static int novatek_fw_check_reset_state(struct spi_device* spi)
+// {
+//     int ret, retry;
+//     u8 buf[5]; /* We only need the data payload */
+// 
+//     /* Vendor used 10 retries for RESET_STATE_INIT */
+//     for (retry = 0; retry < 100; retry++) {
+//         msleep(10);
+// 
+//         ret = nvt_set_page(spi, NVT_EVENT_BUF_ADDR);
+//         if (ret)
+//             continue;
+// 
+//         /* The vendor read 6 bytes total (CMD + DUMMY + 4 DATA). We just ask for 4. */
+//         ret = nvt_spi_read_block(spi, EVENT_MAP_RESET_COMPLETE, buf, 4);
+//         if (ret)
+//             continue;
+// 
+//         /* Check if the state byte falls in the successful boot range */
+//         if (buf[0] >= RESET_STATE_INIT && buf[0] <= RESET_STATE_MAX) {
+//             dev_dbg(&spi->dev, "Novatek MCU booted successfully!\n");
+//             return 0;
+//         }
+//     }
+// 
+//     dev_err(&spi->dev, "Reset state check timed out! state=0x%02X\n", buf[0]);
+//     return -ETIMEDOUT;
+// }
+// 
+// /*static void novatek_read_bld_hw_crc()
+// {
+//     uint8_t buf[8] = { 0 };
+//     uint32_t g_crc = 0, r_crc = 0;
+// 
+//     // CRC Flag
+//     nvt_set_page(chip_info, chip_info->trim_id_table.mmap->BLD_ILM_DLM_CRC_ADDR);
+//     buf[0] = chip_info->trim_id_table.mmap->BLD_ILM_DLM_CRC_ADDR & 0x7F;
+//     buf[1] = 0x00;
+//     CTP_SPI_READ(chip_info->s_client, buf, 2);
+//     TPD_INFO("crc_done = %d, ilm_crc_flag = %d, dlm_crc_flag = %d\n",
+//         (buf[1] >> 2) & 0x01, (buf[1] >> 0) & 0x01, (buf[1] >> 1) & 0x01);
+// 
+//     // ILM CRC
+//     nvt_set_page(chip_info, chip_info->trim_id_table.mmap->G_ILM_CHECKSUM_ADDR);
+//     buf[0] = chip_info->trim_id_table.mmap->G_ILM_CHECKSUM_ADDR & 0x7F;
+//     buf[1] = 0x00;
+//     buf[2] = 0x00;
+//     buf[3] = 0x00;
+//     buf[4] = 0x00;
+//     CTP_SPI_READ(chip_info->s_client, buf, 5);
+//     g_crc = buf[1] | (buf[2] << 8) | (buf[3] << 16) | (buf[4] << 24);
+// 
+//     nvt_set_page(chip_info, chip_info->trim_id_table.mmap->R_ILM_CHECKSUM_ADDR);
+//     buf[0] = chip_info->trim_id_table.mmap->R_ILM_CHECKSUM_ADDR & 0x7F;
+//     buf[1] = 0x00;
+//     buf[2] = 0x00;
+//     buf[3] = 0x00;
+//     buf[4] = 0x00;
+//     CTP_SPI_READ(chip_info->s_client, buf, 5);
+//     r_crc = buf[1] | (buf[2] << 8) | (buf[3] << 16) | (buf[4] << 24);
+// 
+//     TPD_INFO("ilm: bin crc = 0x%08X, golden = 0x%08X, result = 0x%08X\n",
+//         chip_info->bin_map[0].crc, g_crc, r_crc);
+// 
+//     // DLM CRC
+//     nvt_set_page(chip_info, chip_info->trim_id_table.mmap->G_DLM_CHECKSUM_ADDR);
+//     buf[0] = chip_info->trim_id_table.mmap->G_DLM_CHECKSUM_ADDR & 0x7F;
+//     buf[1] = 0x00;
+//     buf[2] = 0x00;
+//     buf[3] = 0x00;
+//     buf[4] = 0x00;
+//     CTP_SPI_READ(chip_info->s_client, buf, 5);
+//     g_crc = buf[1] | (buf[2] << 8) | (buf[3] << 16) | (buf[4] << 24);
+// 
+//     nvt_set_page(chip_info, chip_info->trim_id_table.mmap->R_DLM_CHECKSUM_ADDR);
+//     buf[0] = chip_info->trim_id_table.mmap->R_DLM_CHECKSUM_ADDR & 0x7F;
+//     buf[1] = 0x00;
+//     buf[2] = 0x00;
+//     buf[3] = 0x00;
+//     buf[4] = 0x00;
+//     CTP_SPI_READ(chip_info->s_client, buf, 5);
+//     r_crc = buf[1] | (buf[2] << 8) | (buf[3] << 16) | (buf[4] << 24);
+// 
+//     TPD_INFO("dlm: bin crc = 0x%08X, golden = 0x%08X, result = 0x%08X\n",
+//         chip_info->bin_map[1].crc, g_crc, r_crc);
+// 
+//     return;
+// }*/
+// static void novatek_read_bld_hw_crc(struct spi_device* spi, struct fw_header* fw_data)
+// {
+//     u8 buf[4];
+//     u8 crc_flags;
+//     uint32_t g_crc, r_crc;
+// 
+//     /* Read CRC Flags */
+//     nvt_set_page(spi, NVT_BLD_ILM_DLM_CRC_ADDR);
+//     nvt_spi_read_block(spi, NVT_BLD_ILM_DLM_CRC_ADDR, &crc_flags, 1);
+// 
+//     dev_err(&spi->dev, "CRC Status: done=%d, ILM_err=%d, DLM_err=%d\n",
+//         (crc_flags >> 2) & 0x01, (crc_flags >> 0) & 0x01, (crc_flags >> 1) & 0x01);
+// 
+//     /* Read ILM (Instruction RAM) Checksum */
+//     nvt_set_page(spi, NVT_G_ILM_CHECKSUM_ADDR);
+//     nvt_spi_read_block(spi, NVT_G_ILM_CHECKSUM_ADDR, buf, 4);
+//     g_crc = buf[0] | (buf[1] << 8) | (buf[2] << 16) | (buf[3] << 24);
+// 
+//     nvt_set_page(spi, NVT_R_ILM_CHECKSUM_ADDR);
+//     nvt_spi_read_block(spi, NVT_R_ILM_CHECKSUM_ADDR, buf, 4);
+//     r_crc = buf[0] | (buf[1] << 8) | (buf[2] << 16) | (buf[3] << 24);
+// 
+//     dev_err(&spi->dev, "ILM Checksum: bin=0x%08X, golden=0x%08X, result=0x%08X\n",
+//         fw_data->parts[0].crc, g_crc, r_crc);
+// 
+//     /* Read DLM (Data RAM) Checksum */
+//     nvt_set_page(spi, NVT_G_DLM_CHECKSUM_ADDR);
+//     nvt_spi_read_block(spi, NVT_G_DLM_CHECKSUM_ADDR, buf, 4);
+//     g_crc = buf[0] | (buf[1] << 8) | (buf[2] << 16) | (buf[3] << 24);
+// 
+//     nvt_set_page(spi, NVT_R_DLM_CHECKSUM_ADDR);
+//     nvt_spi_read_block(spi, NVT_R_DLM_CHECKSUM_ADDR, buf, 4);
+//     r_crc = buf[0] | (buf[1] << 8) | (buf[2] << 16) | (buf[3] << 24);
+// 
+//     dev_err(&spi->dev, "DLM Checksum: bin=0x%08X, golden=0x%08X, result=0x%08X\n",
+//         fw_data->parts[1].crc, g_crc, r_crc);
+// }
+// 
+// /*static int novatek_fw_upload(struct firmware* fw, struct spi_device* touch_spi)
+// {
+//     short retry = 0;
+//     short buf[8] = { 0 };
+//     int ret;
+//     bool upload_stop = false;
+// 
+//     while (!upload_stop) {
+//         novatek_esd_check_update_timer();
+// 
+//         novatek_bootloader_reset();
+// 
+//         novatek_set_bld_hw_crc();
+// 
+//         if (cascade_2nd_header_info?????) {
+//             // if second header present - copy in auto copy mode
+//             novatek_tx_auto_copy_mode();
+// 
+//             ret = novatek_fw_write(fw, touch_spi);
+//             if (ret) {
+//                 CTP_SPI_READ(chip_info->s_client, buf, 2);
+//                 TPD_INFO("dummy read = 0x%x\n", buf[1]);
+//                 if (buf[1] == 0xFC) {
+//                     novatek_stop_crc_reboot();
+//                 }
+//                 retry++;
+//                 if (unlikely(retry > 2) || headfile???) {
+//                     dev_err(dev, "fw_upload error, attempt %d\n", retry + 1);
+//                     novatek_read_bld_hw_crc();
+//                     upload_stop = true;
+//                 }
+//                 continue;
+//             } else {
+// 
+//                 ret = novatek_wait_auto_copy();
+//                 if (ret) {
+//                     CTP_SPI_READ(chip_info->s_client, buf, 2);
+//                     TPD_INFO("dummy read = 0x%x\n", buf[1]);
+//                     if (buf[1] == 0xFC) {
+//                         novatek_stop_crc_reboot();
+//                     }
+//                     retry++;
+//                     if (unlikely(retry > 2) || headfile???) {
+//                         dev_err(dev, "fw_upload error, attempt %d\n", retry + 1);
+//                         novatek_read_bld_hw_crc();
+//                         upload_stop = true;
+//                     }
+//                     continue;
+//                 }
+//             }
+//         } else {
+//             ret = novatek_fw_write(fw, touch_spi);
+//             if (ret) {
+//                 CTP_SPI_READ(chip_info->s_client, buf, 2);
+//                 TPD_INFO("dummy read = 0x%x\n", buf[1]);
+//                 if (buf[1] == 0xFC) {
+//                     novatek_stop_crc_reboot();
+//                 }
+//                 retry++;
+//                 if (unlikely(retry > 2) || headfile???) {
+//                     dev_err(dev, "fw_upload error, attempt %d\n", retry + 1);
+//                     novatek_read_bld_hw_crc();
+//                     upload_stop = true;
+//                 }
+//                 continue;
+//             }
+//         }
+// 
+//         novatek_fw_enable_crc();
+// 
+//         novatek_fw_boot_ready();
+// 
+//         ret = novatek_fw_check_reset_state();
+//         if (ret) {
+//             dev_err(dev, "Failed to check reset state of display panel: %d\n", ret);
+//             CTP_SPI_READ(chip_info->s_client, buf, 2);
+//             TPD_INFO("dummy read = 0x%x\n", buf[1]);
+//             if (buf[1] == 0xFC) {
+//                 novatek_stop_crc_reboot();
+//             }
+//             retry++;
+//             if (unlikely(retry > 2) || headfile???) {
+//                 dev_err(dev, "fw_upload error, attempt %d\n", retry + 1);
+//                 novatek_read_bld_hw_crc();
+//                 upload_stop = true;
+//             }
+//             continue;
+//         }
+// 
+//         upload_stop = true;
+//     }
+// 
+//     return 0;
+// }*/
+// static int novatek_fw_upload(struct drm_panel* panel, struct spi_device* spi,
+//     const struct firmware* fw, struct fw_header* fw_data)
+// {
+//     int ret, i;
+// 
+//     /* 1. Halt MCU and wipe pointers */
+//     novatek_bootloader_reset(spi);
+// 
+//     /* 2. Configure ILM/DLM memory banks for the incoming firmware payload */
+//     novatek_set_bld_hw_crc(spi, fw_data);
+// 
+//     /* 3. Handle Cascade (Dual-Chip) Auto-Copy Setup */
+//     if (fw_data->is_cascade) {
+//         ret = novatek_tx_auto_copy_mode(spi, 0x56); // Matches nt36532, which is TX_AUTO_COPY_EN.
+//         if (ret) {
+//             dev_err(panel->dev, "Failed to enable cascade auto-copy mode\n");
+//             return ret;
+//         }
+//     }
+// 
+//     /* 4. Stream the firmware chunks over SPI */
+//     // ret = novatek_fw_write(spi, fw, fw_data);
+//     // if (ret) {
+//     //     dev_err(panel->dev, "Firmware SPI stream failed: %d\n", ret);
+//     //     novatek_stop_crc_reboot(spi); /* Emergency halt to prevent bootlooping */
+//     //     return ret;
+//     // }
+//     /* 4. Stream all partitions */
+//     for (i = 0; i < fw_data->num_partitions; i++) {
+//         ret = novatek_write_partition(spi, fw, &fw_data->parts[i]);
+//         if (ret) {
+//             novatek_stop_crc_reboot(spi);
+//             return ret;
+//         }
+//     }
+// 
+//     /* 5. Wait for Cascade Auto-Copy to finish */
+//     if (fw_data->is_cascade) {
+//         ret = novatek_wait_auto_copy(spi);
+//         if (ret) {
+//             dev_err(panel->dev, "Cascade auto-copy wait failed\n");
+//             novatek_stop_crc_reboot(spi);
+//             return ret;
+//         }
+//     }
+// 
+//     /* 6. Tell the hardware to verify the checksum */
+//     novatek_fw_enable_crc(spi);
+// 
+//     /* 7. Flip the execution switch */
+//     novatek_fw_boot_ready(spi);
+// 
+//     /* 8. Wait for the MCU to confirm it successfully booted the payload */
+//     ret = novatek_fw_check_reset_state(spi);
+//     if (ret) {
+//         dev_err(panel->dev, "Novatek MCU failed to boot the firmware\n");
+// 
+//         /* Print diagnostic hardware error flags to dmesg to see exactly why it failed */
+//         novatek_read_bld_hw_crc(spi, fw_data);
+//         novatek_stop_crc_reboot(spi);
+//         return ret;
+//     }
+// 
+//     return 0;
+// }
+// 
+// /*static int novatek_fw_update(struct drm_panel* panel, struct firmware* fw, struct spi_device* touch_spi)
+// {
+//     struct fw_header fw_data;
+//     // struct touchpanel_data *ts; // That's hell...'
+//     int ret;
+// 
+//     if (fw->size > FW_BUF_SIZE) {
+//         dev_err(panel->dev, "Firmware is larger than %zu bytes. Current fw size: %zu. Refusing to load.\n", FW_BUF_SIZE, fw->size);
+//         return -ENOMEM;
+//     }
+// 
+//     // Check for fw buffer, skipping. For now.
+// 
+//     // Get drvdata from spi s_client. Result - touchpanel_data
+//     // Have monitor_data from chip_info
+// 
+//     // Firmware is not null at this specific call
+//     // but if null - create request_fw_headfile and...
+//     // Check whether firmware is staged or not by checking g_fw_sta(ged) in g_fw_buf, size g_fw_len
+//     // Again, check if firmware in dts... And load it....
+// 
+//     ret = novatek_fw_check_checksum(fw);
+//     if (ret) {
+//         dev_err(panel->dev, "Firmware checksum verification failed.\n");
+//         return -ENODATA;
+//     }
+// 
+//     ret = novatek_fw_parse_header(fw, &fw_data);
+//     if (ret) {
+//         dev_err(panel->dev, "Parsing fw header failed.\n");
+//         return -ENODATA;
+//     }
+// 
+//     // DownloadInit - allocate fwbuf of size NVT_TRANSFER_LEN (1024) + 1 + 1 DUMMY_BYTE. That's it.'
+// 
+//     ret = novatek_fw_upload(fw, touch_spi);
+//     if (ret) {
+//         dev_err(panel->dev, "Firmware upload failed: %d\n", ret);
+//         return -ENODATA;
+//     }
+// 
+//     dev_info(panel->dev, "Firmware updated.\n");
+// 
+//     // CTP_SPI_READ FW info
+//     // If failed: enable esd check.
+// 
+//     novatek_fw_check(...); // Passing touchpanel chip_data, resolution_info and panel_data
+// 
+//     // Freeing up allocated memory.
+// 
+//     return 0;
+// }*/
+// static int novatek_fw_update(struct drm_panel* panel, struct spi_device* touch_spi,
+//     const struct firmware* fw)
+// {
+//     struct fw_header fw_data;
+//     int ret;
+// 
+//     if (fw->size > FW_BUF_SIZE) {
+//         dev_err(panel->dev, "Firmware size %zu exceeds maximum %d bytes\n",
+//             fw->size, FW_BUF_SIZE);
+//         return -ENOMEM;
+//     }
+// 
+//     /* 1. Verify firmware file integrity against vendor checksums */
+//     ret = novatek_fw_check_checksum(panel, fw);
+//     if (ret) {
+//         dev_err(panel->dev, "Firmware checksum verification failed\n");
+//         return -EINVAL;
+//     }
+// 
+//     /* 2. Extract partition maps (ILM/DLM addresses, sizes, and CRCs) */
+//     ret = novatek_fw_parse_header(panel, fw, &fw_data);
+//     if (ret) {
+//         dev_err(panel->dev, "Failed to parse firmware header\n");
+//         return -EINVAL;
+//     }
+// 
+//     /* 3. Execute the physical SPI hardware flash sequence */
+//     ret = novatek_fw_upload(panel, touch_spi, fw, &fw_data);
+//     if (ret) {
+//         dev_err(panel->dev, "Hardware firmware upload sequence failed: %d\n", ret);
+//         return ret;
+//     }
+// 
+//     dev_info(panel->dev, "Novatek firmware successfully flashed and booted.\n");
+// 
+//     return 0;
+// }
+// 
+// static int nt36532_fw_load(struct drm_panel* panel)
+// {
+//     struct panel_info* pinfo = to_panel_info(panel);
+//     const struct firmware* fw;
+//     int ret;
+// 
+//     ret = request_firmware(&fw, "csot-ppc100hb1-1.fw", panel->dev);
+//     if (ret < 0) {
+//         dev_err(panel->dev, "Failed to load firmware csot-ppc100hb1-1.fw: %d\n", ret);
+//         return ret;
+//     }
+// 
+//     dev_info(panel->dev, "Firmware loaded into memory, size: %zu bytes\n", fw->size);
+// 
+//     ret = novatek_fw_update(panel, pinfo->touch_spi, fw);
+// 
+//     if (ret) {
+//         dev_err(panel->dev, "Failed to update firmware: %d\n", ret);
+//         return ret;
+//     }
+// 
+//     // Normal flashing, release
+//     release_firmware(fw);
+// 
+//     return 0;
+// }
+
+// Firmware loading infrastructure end.
+
 static int nt36532_prepare(struct drm_panel* panel)
 {
-    struct nt36532* ctx = to_nt36532(panel);
+    struct panel_info* pinfo = to_panel_info(panel);
     int ret;
 
     ret = regulator_bulk_enable(ARRAY_SIZE(nt36532_supplies),
-        ctx->supplies);
-    if (ret < 0)
+        pinfo->supplies);
+    if (ret < 0) {
+        dev_err(panel->dev, "failed to enable regulators: %d\n", ret);
         return ret;
+    }
 
     /* 20ms delay required by vendor DT for VDDIO stabilization */
     msleep(20);
 
-    nt36532_reset(ctx);
+    nt36532_reset(pinfo);
+
+    // nt36532_fw_load(panel);
 
-    ret = ctx->panel_info->init_sequence(ctx);
+    // msleep(1000);
+
+    ret = pinfo->desc->init_sequence(pinfo);
     if (ret < 0) {
-        gpiod_set_value_cansleep(ctx->reset_gpio, 1);
+        gpiod_set_value_cansleep(pinfo->reset_gpio, 1);
         regulator_bulk_disable(ARRAY_SIZE(nt36532_supplies),
-            ctx->supplies);
+            pinfo->supplies);
+        dev_err(panel->dev, "failed to initialize panel: %d\n", ret);
         return ret;
     }
 
     return 0;
 }
 
-static int nt36532_off(struct nt36532* ctx)
+static int nt36532_disable(struct drm_panel* panel)
 {
-    struct mipi_dsi_device* dsi = ctx->dsi[0];
-    struct mipi_dsi_multi_context dsi_ctx = { .dsi = dsi };
+    struct panel_info* pinfo = to_panel_info(panel);
+    int i;
 
-    mipi_dsi_dcs_set_display_off_multi(&dsi_ctx);
-    mipi_dsi_usleep_range(&dsi_ctx, 10000, 10100);
-    mipi_dsi_dcs_enter_sleep_mode_multi(&dsi_ctx);
-    mipi_dsi_msleep(&dsi_ctx, 65);
+    for (i = 0; i < DSI_NUM_MIN + pinfo->desc->is_dual_dsi; i++) {
+        struct mipi_dsi_multi_context dsi_pinfo = { .dsi = pinfo->dsi[i] };
+        mipi_dsi_dcs_set_display_off_multi(&dsi_pinfo);
+    }
+    usleep_range(10000, 10100);
+    for (i = 0; i < DSI_NUM_MIN + pinfo->desc->is_dual_dsi; i++) {
+        struct mipi_dsi_multi_context dsi_pinfo = { .dsi = pinfo->dsi[i] };
+        mipi_dsi_dcs_enter_sleep_mode_multi(&dsi_pinfo);
+    }
+    msleep(65);
 
-    return dsi_ctx.accum_err;
+    return 0;
 }
 
 static int nt36532_unprepare(struct drm_panel* panel)
 {
-    struct nt36532* ctx = to_nt36532(panel);
-    struct device* dev = &ctx->dsi[0]->dev;
-    int ret;
+    struct panel_info* pinfo = to_panel_info(panel);
 
-    ret = nt36532_off(ctx);
-    if (ret < 0)
-        dev_err(dev, "Failed to un-initialize panel: %d\n", ret);
-
-    gpiod_set_value_cansleep(ctx->reset_gpio, 1);
-    regulator_bulk_disable(ARRAY_SIZE(nt36532_supplies), ctx->supplies);
+    gpiod_set_value_cansleep(pinfo->reset_gpio, 1);
+    // gpiod_set_value_cansleep(pinfo->mcu_reset_gpio, 1);
+    regulator_bulk_disable(ARRAY_SIZE(nt36532_supplies), pinfo->supplies);
 
     return 0;
 }
 
+static void nt36532_remove(struct mipi_dsi_device* dsi)
+{
+    struct panel_info* pinfo = mipi_dsi_get_drvdata(dsi);
+    // put_device(&pinfo->touch_spi->dev);
+
+    drm_panel_remove(&pinfo->panel);
+}
+
 static int nt36532_get_modes(struct drm_panel* panel,
     struct drm_connector* connector)
 {
-    struct nt36532* ctx = to_nt36532(panel);
-    const struct panel_info* panel_info = ctx->panel_info;
+    struct panel_info* pinfo = to_panel_info(panel);
+    int i;
+
+    for (i = 0; i < pinfo->desc->num_modes; i++) {
+        const struct drm_display_mode* m = &pinfo->desc->modes[i];
+        struct drm_display_mode* mode;
+
+        mode = drm_mode_duplicate(connector->dev, m);
+        if (!mode) {
+            dev_err(panel->dev, "failed to add mode %ux%u@%u\n",
+                m->hdisplay, m->vdisplay, drm_mode_vrefresh(m));
+            return -ENOMEM;
+        }
+
+        mode->type = DRM_MODE_TYPE_DRIVER;
+        if (i == 0)
+            mode->type |= DRM_MODE_TYPE_PREFERRED;
+
+        drm_mode_set_name(mode);
+        drm_mode_probed_add(connector, mode);
+    }
+
+    connector->display_info.width_mm = pinfo->desc->width_mm;
+    connector->display_info.height_mm = pinfo->desc->height_mm;
+    connector->display_info.bpc = pinfo->desc->bpc;
+
+    return pinfo->desc->num_modes;
+}
+
+static enum drm_panel_orientation nt36532_get_orientation(struct drm_panel* panel)
+{
+    struct panel_info* pinfo = to_panel_info(panel);
 
-    return drm_connector_helper_get_modes_fixed(connector,
-        &panel_info->display_mode);
+    return pinfo->orientation;
 }
 
 static const struct drm_panel_funcs nt36532_panel_funcs = {
+    .disable = nt36532_disable,
     .prepare = nt36532_prepare,
     .unprepare = nt36532_unprepare,
     .get_modes = nt36532_get_modes,
+    .get_orientation = nt36532_get_orientation,
 };
 
+static int nt36532_bl_update_status(struct backlight_device* bl)
+{
+    struct mipi_dsi_device* dsi = bl_get_data(bl);
+    u16 brightness = backlight_get_brightness(bl);
+    int ret;
+
+    dsi->mode_flags &= ~MIPI_DSI_MODE_LPM;
+
+    ret = mipi_dsi_dcs_set_display_brightness_large(dsi, brightness);
+    if (ret < 0)
+        return ret;
+
+    dsi->mode_flags |= MIPI_DSI_MODE_LPM;
+
+    return 0;
+}
+
+static int nt36532_bl_get_brightness(struct backlight_device* bl)
+{
+    struct mipi_dsi_device* dsi = bl_get_data(bl);
+    u16 brightness;
+    int ret;
+
+    dsi->mode_flags &= ~MIPI_DSI_MODE_LPM;
+
+    ret = mipi_dsi_dcs_get_display_brightness_large(dsi, &brightness);
+    if (ret < 0)
+        return ret;
+
+    dsi->mode_flags |= MIPI_DSI_MODE_LPM;
+
+    return brightness;
+}
+
+static const struct backlight_ops nt36532_bl_ops = {
+    .update_status = nt36532_bl_update_status,
+    .get_brightness = nt36532_bl_get_brightness,
+};
+
+static struct backlight_device* nt36532_create_backlight(struct mipi_dsi_device* dsi)
+{
+    struct device* dev = &dsi->dev;
+    const struct backlight_properties props = {
+        .type = BACKLIGHT_RAW,
+        .brightness = 512,
+        .max_brightness = 2047,
+        .scale = BACKLIGHT_SCALE_NON_LINEAR,
+    };
+
+    return devm_backlight_device_register(dev, dev_name(dev), dev, dsi,
+        &nt36532_bl_ops, &props);
+}
+
 static int nt36532_probe(struct mipi_dsi_device* dsi)
 {
-    struct mipi_dsi_device_info dsi_info = { "nt36532-secondary", 0, NULL };
-    const struct panel_info* panel_info;
-    struct mipi_dsi_host* dsi1_host;
     struct device* dev = &dsi->dev;
     struct device_node* dsi1;
-    struct nt36532* ctx;
-    int num_dsi = 1;
+    struct mipi_dsi_host* dsi1_host;
+    struct panel_info* pinfo;
+    const struct mipi_dsi_device_info* info;
+    // struct device_node* spi_node;
+    // struct device* spi_dev;
     int ret, i;
 
-    ctx = devm_kzalloc(dev, sizeof(*ctx), GFP_KERNEL);
-    if (!ctx)
-        return -ENOMEM;
+    pinfo = devm_drm_panel_alloc(dev, struct panel_info, panel,
+        &nt36532_panel_funcs,
+        DRM_MODE_CONNECTOR_DSI);
+    if (IS_ERR(pinfo))
+        return PTR_ERR(pinfo);
 
-    ret = devm_regulator_bulk_get_const(&dsi->dev,
+    ret = devm_regulator_bulk_get_const(dev,
         ARRAY_SIZE(nt36532_supplies),
-        nt36532_supplies, &ctx->supplies);
-    if (ret < 0)
+        nt36532_supplies, &pinfo->supplies);
+    if (ret < 0) {
+        dev_err(dev, "failed to get all regulators: %d\n", ret);
         return ret;
+    }
 
-    ctx->reset_gpio = devm_gpiod_get(dev, "reset", GPIOD_OUT_LOW);
-    if (IS_ERR(ctx->reset_gpio))
-        return dev_err_probe(dev, PTR_ERR(ctx->reset_gpio),
-            "Failed to get reset-gpios\n");
-
-    ctx->panel_info = of_device_get_match_data(dev);
-    panel_info = ctx->panel_info;
-    if (!panel_info)
+    pinfo->reset_gpio = devm_gpiod_get(dev, "reset", GPIOD_OUT_LOW);
+    if (IS_ERR(pinfo->reset_gpio))
+        return dev_err_probe(dev, PTR_ERR(pinfo->reset_gpio),
+            "Failed to get reset-gpio\n");
+
+    // pinfo->mcu_reset_gpio = devm_gpiod_get_index(dev, "reset", 1, GPIOD_OUT_LOW);
+    // if (IS_ERR(pinfo->mcu_reset_gpio))
+    //     return dev_err_probe(dev, PTR_ERR(pinfo->mcu_reset_gpio),
+    //         "Failed to get mcu-reset-gpio\n");
+
+    pinfo->desc = of_device_get_match_data(dev);
+    if (!pinfo->desc)
         return -ENODEV;
 
-    if (panel_info->is_dual_dsi) {
-        num_dsi = 2;
+    if (pinfo->desc->is_dual_dsi) {
+        info = &pinfo->desc->dsi_info;
+
         dsi1 = of_graph_get_remote_node(dsi->dev.of_node, 1, -1);
         if (!dsi1) {
             dev_err(dev, "cannot get secondary DSI node.\n");
@@ -186,37 +1926,73 @@
             return dev_err_probe(dev, -EPROBE_DEFER,
                 "cannot get secondary DSI host\n");
 
-        dsi_info.node = dsi1;
-
-        ctx->dsi[1] = devm_mipi_dsi_device_register_full(dev, dsi1_host,
-            &dsi_info);
-        if (IS_ERR(ctx->dsi[1])) {
+        pinfo->dsi[1] = devm_mipi_dsi_device_register_full(dev, dsi1_host,
+            info);
+        if (IS_ERR(pinfo->dsi[1])) {
             dev_err(dev, "cannot get secondary DSI device\n");
-            return PTR_ERR(ctx->dsi[1]);
+            return PTR_ERR(pinfo->dsi[1]);
         }
 
-        mipi_dsi_set_drvdata(ctx->dsi[1], ctx);
+        mipi_dsi_set_drvdata(pinfo->dsi[1], pinfo);
     }
 
-    ctx->dsi[0] = dsi;
-    mipi_dsi_set_drvdata(dsi, ctx);
+    pinfo->dsi[0] = dsi;
+    mipi_dsi_set_drvdata(dsi, pinfo);
 
-    drm_panel_init(&ctx->panel, dev, &nt36532_panel_funcs,
-        DRM_MODE_CONNECTOR_DSI);
+    ret = of_drm_get_panel_orientation(dev->of_node, &pinfo->orientation);
+    if (ret < 0) {
+        dev_err(dev, "%pOF: failed to get orientation %d\n", dev->of_node, ret);
+        return ret;
+    }
+
+    pinfo->panel.prepare_prev_first = true;
 
-    ctx->panel.prepare_prev_first = true;
+    if (pinfo->desc->has_dcs_backlight) {
+        pinfo->panel.backlight = nt36532_create_backlight(dsi);
+        if (IS_ERR(pinfo->panel.backlight))
+            return dev_err_probe(dev, PTR_ERR(pinfo->panel.backlight),
+                "Failed to create backlight\n");
+    } else {
+        ret = drm_panel_of_backlight(&pinfo->panel);
+        if (ret)
+            return dev_err_probe(dev, ret, "Failed to get backlight\n");
+    }
+
+    // spi_node = of_parse_phandle(dev->of_node, "touch-controller", 0);
+    // if (!spi_node) {
+    //     return dev_err_probe(dev, -ENODEV, "Failed to find touch SPI node in DT\n");
+    // }
+
+    // spi_dev = bus_find_device_by_of_node(&spi_bus_type, spi_node);
+    // of_node_put(spi_node);
+
+    // if (!spi_dev) {
+    //     return dev_err_probe(dev, -EPROBE_DEFER, "SPI device not ready yet\n");
+    // }
+
+    // /* Cache it for the firmware flashing sequence! */
+    // pinfo->touch_spi = to_spi_device(spi_dev);
+
+    drm_panel_add(&pinfo->panel);
 
-    drm_panel_add(&ctx->panel);
+    if (pinfo->desc->dsc_cfg) {
+        pinfo->desc->dsc_cfg->slice_count = dsc_slice_count;
+        pinfo->desc->dsc_cfg->slice_width = dsc_slice_width;
+        pinfo->desc->dsc_cfg->slice_height = dsc_slice_height;
+        pinfo->desc->dsc_cfg->dsc_version_minor = dsc_version_minor;
+    }
+
+    for (i = 0; i < DSI_NUM_MIN + pinfo->desc->is_dual_dsi; i++) {
+        pinfo->dsi[i]->lanes = pinfo->desc->lanes;
+        pinfo->dsi[i]->format = pinfo->desc->format;
+        pinfo->dsi[i]->mode_flags = pinfo->desc->mode_flags;
+        pinfo->dsi[i]->dsc = pinfo->desc->dsc_cfg;
+        pinfo->dsi[i]->dsc_slice_per_pkt = dsc_slice_per_pkt; // pinfo->desc->dsc_slice_per_pkt;
+        pinfo->dsi[i]->hs_rate = pinfo->desc->hs_rate;
 
-    for (i = 0; i < num_dsi; i++) {
-        ctx->dsi[i]->lanes = panel_info->lanes;
-        ctx->dsi[i]->format = panel_info->format;
-        ctx->dsi[i]->mode_flags = panel_info->mode_flags;
-        ctx->dsi[i]->dsc = panel_info->dsc_cfg;
-        ctx->dsi[i]->dsc_slice_per_pkt = panel_info->dsc_slice_per_pkt;
-        ret = devm_mipi_dsi_attach(dev, ctx->dsi[i]);
+        ret = devm_mipi_dsi_attach(dev, pinfo->dsi[i]);
         if (ret < 0) {
-            drm_panel_remove(&ctx->panel);
+            drm_panel_remove(&pinfo->panel);
             return dev_err_probe(dev, ret,
                 "Failed to attach to DSI host\n");
         }
@@ -225,177 +2001,313 @@
     return 0;
 }
 
-static void nt36532_remove(struct mipi_dsi_device* dsi)
-{
-    struct nt36532* ctx = mipi_dsi_get_drvdata(dsi);
-
-    drm_panel_remove(&ctx->panel);
-}
-
-static int csot_init_sequence(struct nt36532* ctx)
+static int csot_init_sequence(struct panel_info* pinfo)
 {
-    struct mipi_dsi_device* dsi = ctx->dsi[0];
+    struct mipi_dsi_device* dsi0 = pinfo->dsi[0];
+    struct mipi_dsi_device* dsi1 = pinfo->dsi[1];
     struct drm_dsc_picture_parameter_set pps;
-    struct mipi_dsi_multi_context dsi_ctx = { .dsi = dsi };
+    struct mipi_dsi_multi_context dsi_pinfo = { .dsi = NULL };
+    // u8 pwr_mode;
+
+    msleep(20);
 
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xff, 0x22);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xfb, 0x01);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_WRITE_LUT, 0x01);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xff, 0x24);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xfb, 0x01);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xbc,
+    // Testing that the link is alive.
+    // int ret = mipi_dsi_dcs_get_power_mode(dsi0, &pwr_mode);
+    // if (ret < 0)
+    //     pr_err("NOVA-DEBUG: BTA Read failed with error %d\n", ret);
+    // else
+    //     pr_err("NOVA-DEBUG: Power Mode at boot = 0x%02x\n", pwr_mode);
+
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0xff, 0x22);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0xfb, 0x01);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_WRITE_LUT, 0x01);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0xff, 0x24);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0xfb, 0x01);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0xbc,
         0x00, 0x00, 0x03, 0x22, 0x00, 0x41);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xff, 0x23);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xfb, 0x01);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_NOP, 0x60);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_GET_GREEN_CHANNEL, 0x20);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_GET_BLUE_CHANNEL, 0x01);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_GET_DISPLAY_STATUS, 0x5a);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_ENTER_SLEEP_MODE, 0x0c);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_EXIT_SLEEP_MODE, 0x03);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_ENTER_PARTIAL_MODE, 0xe1);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_GET_IMAGE_CHECKSUM_CT, 0xa9);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DSI_EXECUTE_QUEUE, 0x16);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0xff, 0x23);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0xfb, 0x01);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_NOP, 0x60);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_GET_GREEN_CHANNEL, 0x20);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_GET_BLUE_CHANNEL, 0x01);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_GET_DISPLAY_STATUS, 0x5a);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_ENTER_SLEEP_MODE, 0x0c);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_EXIT_SLEEP_MODE, 0x03);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_ENTER_PARTIAL_MODE, 0xe1);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_GET_IMAGE_CHECKSUM_CT, 0xa9);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DSI_EXECUTE_QUEUE, 0x16);
     // ---------------
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_GET_POWER_MODE, 0x8e);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_GET_ADDRESS_MODE, 0x8e);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_GET_PIXEL_FORMAT, 0x8e);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_GET_DISPLAY_MODE, 0x00);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DSI_BLANKING_PACKET, 0x00);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DSI_RX_GENERIC_LONG_READ_RESPONSE, 0x04);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_GET_POWER_MODE, 0x8e);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_GET_ADDRESS_MODE, 0x8e);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_GET_PIXEL_FORMAT, 0x8e);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_GET_DISPLAY_MODE, 0x00);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DSI_BLANKING_PACKET, 0x00);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DSI_RX_GENERIC_LONG_READ_RESPONSE, 0x04);
     // ---------------
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x1b, 0x08);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DSI_RX_DCS_LONG_READ_RESPONSE, 0x0c);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DSI_PACKED_PIXEL_STREAM_36, 0x10);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DSI_PACKED_PIXEL_STREAM_18, 0x14);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x1f, 0x18);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_EXIT_INVERT_MODE, 0x1c);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_ENTER_INVERT_MODE, 0x20);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DSI_SHUTDOWN_PERIPHERAL, 0x24);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DSI_GENERIC_SHORT_WRITE_2_PARAM, 0x28);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DSI_GENERIC_READ_REQUEST_2_PARAM, 0x2c);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x25, 0x30);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_SET_GAMMA_CURVE, 0x34);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x27, 0x38);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_SET_DISPLAY_OFF, 0x3c);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_SET_COLUMN_ADDRESS, 0x20);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_SET_PAGE_ADDRESS, 0x20);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_SET_PARTIAL_ROWS, 0xff);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_SET_PARTIAL_COLUMNS,
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x1b, 0x08);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DSI_RX_DCS_LONG_READ_RESPONSE, 0x0c);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DSI_PACKED_PIXEL_STREAM_36, 0x10);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DSI_PACKED_PIXEL_STREAM_18, 0x14);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x1f, 0x18);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_EXIT_INVERT_MODE, 0x1c);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_ENTER_INVERT_MODE, 0x20);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DSI_SHUTDOWN_PERIPHERAL, 0x24);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DSI_GENERIC_SHORT_WRITE_2_PARAM, 0x28);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DSI_GENERIC_READ_REQUEST_2_PARAM, 0x2c);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x25, 0x30);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_SET_GAMMA_CURVE, 0x34);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x27, 0x38);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_SET_DISPLAY_OFF, 0x3c);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_SET_COLUMN_ADDRESS, 0x20);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_SET_PAGE_ADDRESS, 0x20);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_SET_PARTIAL_ROWS, 0xff);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_SET_PARTIAL_COLUMNS,
         0xfd);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x32, 0xfc);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_SET_SCROLL_AREA, 0xfa);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_SET_TEAR_OFF, 0xf8);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_SET_TEAR_ON, 0xf6);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_SET_ADDRESS_MODE, 0xf4);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_SET_SCROLL_START, 0xf2);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_EXIT_IDLE_MODE, 0xf0);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_ENTER_IDLE_MODE, 0xee);
-    mipi_dsi_dcs_set_pixel_format_multi(&dsi_ctx, 0xec); // 0x3a
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x3b, 0xea);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_SET_3D_CONTROL, 0xe9);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_GET_3D_CONTROL, 0xe8);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_SET_VSYNC_TIMING, 0xe7);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x41, 0xe6);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_GET_SCANLINE, 0xff);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x46, 0xf9);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x47, 0xf6);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x48, 0xf2);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x49, 0xf0);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x4a, 0xec);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x4b, 0xe8);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x4c, 0xe4);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x4d, 0xe0);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x4e, 0xde);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x4f, 0xd9);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x50, 0xd6);
-    mipi_dsi_dcs_set_display_brightness_multi(&dsi_ctx, 0x00d4); // 0x51
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_GET_DISPLAY_BRIGHTNESS, 0xc2);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_WRITE_CONTROL_DISPLAY, 0xd0);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_GET_CONTROL_DISPLAY, 0xcd);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x58, 0xff);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x59, 0xf6);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x5a, 0xf0);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x5b, 0xeb);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x5c, 0xe8);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x5d, 0xe5);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_SET_CABC_MIN_BRIGHTNESS, 0xe3);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_GET_CABC_MIN_BRIGHTNESS, 0xe0);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x60, 0xde);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x61, 0xda);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x62, 0xd7);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x63, 0xd4);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x64, 0xd2);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x65, 0xd0);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x66, 0xcc);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x67, 0xc8);
-
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xff, 0x27);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xfb, 0x01);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_SET_PARTIAL_ROWS, 0xf0);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_SET_PARTIAL_COLUMNS,
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x32, 0xfc);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_SET_SCROLL_AREA, 0xfa);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_SET_TEAR_OFF, 0xf8);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_SET_TEAR_ON, 0xf6);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_SET_ADDRESS_MODE, 0xf4);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_SET_SCROLL_START, 0xf2);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_EXIT_IDLE_MODE, 0xf0);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_ENTER_IDLE_MODE, 0xee);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_SET_PIXEL_FORMAT, 0xec); // 0x3a
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x3b, 0xea);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_SET_3D_CONTROL, 0xe9);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_GET_3D_CONTROL, 0xe8);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_SET_VSYNC_TIMING, 0xe7);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x41, 0xe6);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_GET_SCANLINE, 0xff);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x46, 0xf9);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x47, 0xf6);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x48, 0xf2);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x49, 0xf0);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x4a, 0xec);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x4b, 0xe8);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x4c, 0xe4);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x4d, 0xe0);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x4e, 0xde);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x4f, 0xd9);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x50, 0xd6);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_SET_DISPLAY_BRIGHTNESS, 0x00d4); // 0x51
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_GET_DISPLAY_BRIGHTNESS, 0xc2);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_WRITE_CONTROL_DISPLAY, 0xd0);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_GET_CONTROL_DISPLAY, 0xcd);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x58, 0xff);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x59, 0xf6);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x5a, 0xf0);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x5b, 0xeb);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x5c, 0xe8);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x5d, 0xe5);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_SET_CABC_MIN_BRIGHTNESS, 0xe3);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_GET_CABC_MIN_BRIGHTNESS, 0xe0);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x60, 0xde);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x61, 0xda);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x62, 0xd7);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x63, 0xd4);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x64, 0xd2);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x65, 0xd0);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x66, 0xcc);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x67, 0xc8);
+
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0xff, 0x27);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0xfb, 0x01);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_SET_PARTIAL_ROWS, 0xf0);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_SET_PARTIAL_COLUMNS,
         0x2a, 0x00);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xd0, 0x31);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xd1, 0x54);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xde, 0x40);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xdf, 0x02);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xff, 0x2a);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xfb, 0x01);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xc4, 0x02);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xff, 0xf0);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xfb, 0x01);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xfa, 0x05);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x76, 0x16);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xff, 0x23);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xfb, 0x01);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xba, 0xaa, 0x2a);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xbb, 0xa0, 0x2a);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xff, 0x27);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xfb, 0x01);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_SOFT_RESET, 0x8c);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xff, 0x10);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xfb, 0x01);
-    mipi_dsi_dcs_set_display_brightness_multi(&dsi_ctx, 0xff07); // 0x51
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_WRITE_CONTROL_DISPLAY, 0x2c);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xb9, 0x00);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_WRITE_POWER_SAVE, 0x01);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xb9, 0x02);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x3b,
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0xd0, 0x31);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0xd1, 0x54);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0xde, 0x40);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0xdf, 0x02);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0xff, 0x2a);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0xfb, 0x01);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0xc4, 0x02);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0xff, 0xf0);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0xfb, 0x01);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0xfa, 0x05);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x76, 0x16);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0xff, 0x23);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0xfb, 0x01);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0xba, 0xaa, 0x2a);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0xbb, 0xa0, 0x2a);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0xff, 0x27);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0xfb, 0x01);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_SOFT_RESET, 0x8c);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0xff, 0x10);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0xfb, 0x01);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_SET_DISPLAY_BRIGHTNESS, 0x07, 0xff); // 0x51
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_WRITE_CONTROL_DISPLAY, 0x2c);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0xb9, 0x00);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_WRITE_POWER_SAVE, 0x01);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0xb9, 0x02);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x3b,
         0x03, 0xd2, 0x1a, 0x04, 0x04, 0x00);
 
     /* Enable DSC */
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x90, 0x03);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x90, 0x03);
+
+    // mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x91,
+    //     0x89, 0xa8, 0x00, 0x14, 0xd2, 0x00, 0x00, 0x00, 0x02, 0x1e, 0x00, 0x0a, 0x05, 0x7a, 0x03, 0xaa);
+    drm_dsc_pps_payload_pack(&pps, pinfo->desc->dsc_cfg); // command 0x91
+    dsi_pinfo.dsi = dsi0;
+    mipi_dsi_picture_parameter_set_multi(&dsi_pinfo, &pps);
+
+    if (pinfo->desc->is_dual_dsi) {
+        dsi_pinfo.dsi = dsi1;
+        mipi_dsi_picture_parameter_set_multi(&dsi_pinfo, &pps);
+    }
+
+    dsi_pinfo.dsi = NULL;
 
-    drm_dsc_pps_payload_pack(&pps, ctx->panel_info->dsc_cfg); // command 0x91
-    mipi_dsi_picture_parameter_set_multi(&dsi_ctx, &pps); // command 0x92
+    // mipi_dsi_picture_parameter_set_multi(&dsi_pinfo, &pps); // command 0x92
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x92, 0x10, 0xf0);
 
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x9d, 0x01);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0x9d, 0x01);
 
     /* Program refresh rate control registers */
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xb2, 0x91);
-    mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xb3, 0x40); // Fix: was 0x41, vendor tree - 0x40
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0xb2, 0x91);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, 0xb3, 0x41); // Fix: was 0x41, vendor tree - 0x40
 
-    mipi_dsi_dcs_exit_sleep_mode_multi(&dsi_ctx); // 0x11
-    mipi_dsi_msleep(&dsi_ctx, 120 /*0x78*/);
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_EXIT_SLEEP_MODE, 0x00); // 0x11
+    mipi_dsi_msleep(&dsi_pinfo, 120 /*0x78*/);
+
+    mipi_dsi_dual_dcs_write_seq_multi(&dsi_pinfo, dsi0, dsi1, MIPI_DCS_SET_DISPLAY_ON, 0x00); // 0x29
+    mipi_dsi_usleep_range(&dsi_pinfo, 10000, 10100); // 0xa0 usleep range
+
+    msleep(120);
+    // u8 buf[1] = { 0 };
+    // int ret2;
+    //
+    ///* Read Display Power Mode (Command 0x0A) */
+    // ret2 = mipi_dsi_dcs_read(dsi0, MIPI_DCS_GET_POWER_MODE, buf, 1);
+    // if (ret2 >= 0)
+    //     pr_err("DEBUG-DSI0: Power Mode (0x0A) = 0x%02x\n", buf[0]);
+    ///* Read Display Power Mode (Command 0x0A) */
+    // ret2 = mipi_dsi_dcs_read(dsi1, MIPI_DCS_GET_POWER_MODE, buf, 1);
+    // if (ret2 >= 0)
+    //     pr_err("DEBUG-DSI1: Power Mode (0x0A) = 0x%02x\n", buf[0]);
+    //
+    ///* Read Display Brightness (Command 0x52) */
+    // ret2 = mipi_dsi_dcs_read(dsi0, MIPI_DCS_GET_DISPLAY_BRIGHTNESS, buf, 1);
+    // if (ret2 >= 0)
+    //     pr_err("DEBUG-DSI0: Brightness (0x52) = 0x%02x\n", buf[0]);
+    ///* Read Display Brightness (Command 0x52) */
+    // ret2 = mipi_dsi_dcs_read(dsi1, MIPI_DCS_GET_DISPLAY_BRIGHTNESS, buf, 1);
+    // if (ret2 >= 0)
+    //     pr_err("DEBUG-DSI1: Brightness (0x52) = 0x%02x\n", buf[0]);
+    //
+    ///* Read Control Display (Command 0x54) */
+    // ret2 = mipi_dsi_dcs_read(dsi0, MIPI_DCS_GET_CONTROL_DISPLAY, buf, 1);
+    // if (ret2 >= 0)
+    //     pr_err("DEBUG-DSI0: Ctrl Display (0x54) = 0x%02x\n", buf[0]);
+    ///* Read Control Display (Command 0x54) */
+    // ret2 = mipi_dsi_dcs_read(dsi1, MIPI_DCS_GET_CONTROL_DISPLAY, buf, 1);
+    // if (ret2 >= 0)
+    //     pr_err("DEBUG-DSI1: Ctrl Display (0x54) = 0x%02x\n", buf[0]);
+
+    return dsi_pinfo.accum_err;
+}
 
-    mipi_dsi_dcs_set_display_on_multi(&dsi_ctx); // 0x29
-    mipi_dsi_usleep_range(&dsi_ctx, 10000, 10100); // 0xa0 usleep range
+static const struct drm_display_mode csot_display_modes[] = {
+    /* 120 Hz (Preferred Default) - Base Clock */
+    {
+        .flags = 0,
+        .name = "3000x2120@120vid",
+        .clock = (3000 + 236 + 40 + 200) * (2120 + 26 + 2 + 208) * 120 / 1000,
+        .hdisplay = 3000,
+        .hsync_start = 3000 + 236,
+        .hsync_end = 3000 + 236 + 40,
+        .htotal = 3000 + 236 + 40 + 200,
+        .vdisplay = 2120,
+        .vsync_start = 2120 + 26,
+        .vsync_end = 2120 + 26 + 2,
+        .vtotal = 2120 + 26 + 2 + 208,
+    },
+    /* 144 Hz - High Speed Clock (Unique Horizontal Timings) */
+    {
+        .flags = 0,
+        .name = "3000x2120@144vid",
+        .clock = (3000 + 114 + 40 + 76) * (2120 + 26 + 2 + 208) * 144 / 1000,
+        .hdisplay = 3000,
+        .hsync_start = 3000 + 114,
+        .hsync_end = 3000 + 114 + 40,
+        .htotal = 3000 + 114 + 40 + 76,
+        .vdisplay = 2120,
+        .vsync_start = 2120 + 26,
+        .vsync_end = 2120 + 26 + 2,
+        .vtotal = 2120 + 26 + 2 + 208,
+    },
+    /* 90 Hz - VFP Stretch */
+    {
+        .flags = 0,
+        .name = "3000x2120@90vid",
+        .clock = (3000 + 236 + 40 + 200) * (2120 + 811 + 2 + 208) * 90 / 1000,
+        .hdisplay = 3000,
+        .hsync_start = 3000 + 236,
+        .hsync_end = 3000 + 236 + 40,
+        .htotal = 3000 + 236 + 40 + 200,
+        .vdisplay = 2120,
+        .vsync_start = 2120 + 811,
+        .vsync_end = 2120 + 811 + 2,
+        .vtotal = 2120 + 811 + 2 + 208,
+    },
+    /* 60 Hz - VFP Stretch */
+    {
+        .flags = 0,
+        .name = "3000x2120@60vid",
+        .clock = (3000 + 236 + 40 + 200) * (2120 + 2382 + 2 + 208) * 60 / 1000,
+        .hdisplay = 3000,
+        .hsync_start = 3000 + 236,
+        .hsync_end = 3000 + 236 + 40,
+        .htotal = 3000 + 236 + 40 + 200,
+        .vdisplay = 2120,
+        .vsync_start = 2120 + 2382,
+        .vsync_end = 2120 + 2382 + 2,
+        .vtotal = 2120 + 2382 + 2 + 208,
+    },
+    /* 50 Hz - VFP Stretch */
+    {
+        .flags = 0,
+        .name = "3000x2120@50vid",
+        .clock = (3000 + 236 + 40 + 200) * (2120 + 3324 + 2 + 208) * 50 / 1000,
+        .hdisplay = 3000,
+        .hsync_start = 3000 + 236,
+        .hsync_end = 3000 + 236 + 40,
+        .htotal = 3000 + 236 + 40 + 200,
+        .vdisplay = 2120,
+        .vsync_start = 2120 + 3324,
+        .vsync_end = 2120 + 3324 + 2,
+        .vtotal = 2120 + 3324 + 2 + 208,
+    },
+    /* 48 Hz - VFP Stretch */
+    {
+        .flags = 0,
+        .name = "3000x2120@48vid",
+        .clock = (3000 + 236 + 40 + 200) * (2120 + 3560 + 2 + 208) * 48 / 1000,
+        .hdisplay = 3000,
+        .hsync_start = 3000 + 236,
+        .hsync_end = 3000 + 236 + 40,
+        .htotal = 3000 + 236 + 40 + 200,
+        .vdisplay = 2120,
+        .vsync_start = 2120 + 3560,
+        .vsync_end = 2120 + 3560 + 2,
+        .vtotal = 2120 + 3560 + 2 + 208,
+    },
+    /* 30 Hz - VFP Stretch */
+    {
+        .flags = 0,
+        .name = "3000x2120@30vid",
+        .clock = (3000 + 236 + 40 + 200) * (2120 + 7094 + 2 + 208) * 30 / 1000,
+        .hdisplay = 3000,
+        .hsync_start = 3000 + 236,
+        .hsync_end = 3000 + 236 + 40,
+        .htotal = 3000 + 236 + 40 + 200,
+        .vdisplay = 2120,
+        .vsync_start = 2120 + 7094,
+        .vsync_end = 2120 + 7094 + 2,
+        .vtotal = 2120 + 7094 + 2 + 208,
+    },
 
-    return dsi_ctx.accum_err;
-}
-
-static const struct drm_display_mode csot_display_mode = {
-    .flags = DRM_MODE_FLAG_NHSYNC | DRM_MODE_FLAG_NVSYNC, // Possibly panel has negative hsync and vsync. Will check it.
-    /* 120Hz, the typical value */
-    .clock = (1500 + 118 + 20 + 100) * 2 * (2120 + 26 + 2 + 208) * 120 / 1000 * 0.9962, // Dirty hack to fit 514 MHz limit of mainline, will investigate later
-    .hdisplay = 1500 * 2,
-    .hsync_start = (1500 + 118) * 2,
-    .hsync_end = (1500 + 118 + 20) * 2,
-    .htotal = (1500 + 118 + 20 + 100) * 2,
-    .vdisplay = 2120,
-    .vsync_start = 2120 + 26,
-    .vsync_end = 2120 + 26 + 2,
-    .vtotal = 2120 + 26 + 2 + 208,
 };
 
 static struct drm_dsc_config csot_dsc_cfg = {
@@ -409,21 +2321,30 @@
     .block_pred_enable = true,
 };
 
-static const struct panel_info csot_panel_info = {
+static const struct panel_desc csot_panel_desc = {
+    .dsi_info = {
+        .type = "csot-ppc100hb1-1",
+        .channel = 0,
+        .node = NULL,
+    },
     .width_mm = 250,
     .height_mm = 177,
+    .bpc = 8,
     .lanes = 4,
     .format = MIPI_DSI_FMT_RGB888,
-    .mode_flags = MIPI_DSI_MODE_VIDEO | MIPI_DSI_CLOCK_NON_CONTINUOUS | MIPI_DSI_MODE_LPM,
-    .display_mode = csot_display_mode,
+    .mode_flags = MIPI_DSI_MODE_VIDEO /*| MIPI_DSI_CLOCK_NON_CONTINUOUS*/ | MIPI_DSI_MODE_LPM,
+    .modes = csot_display_modes,
+    .num_modes = ARRAY_SIZE(csot_display_modes),
     .dsc_slice_per_pkt = 2,
     .dsc_cfg = &csot_dsc_cfg,
+    .hs_rate = 1254876800,
     .init_sequence = csot_init_sequence,
     .is_dual_dsi = true,
+    .has_dcs_backlight = false,
 };
 
 static const struct of_device_id nt36532_of_match[] = {
-    { .compatible = "csot,ppc100hb1-1", .data = &csot_panel_info },
+    { .compatible = "csot,ppc100hb1-1", .data = &csot_panel_desc },
     { /* sentinel */ }
 };
 MODULE_DEVICE_TABLE(of, nt36532_of_match);
@@ -439,5 +2360,6 @@
 module_mipi_dsi_driver(nt36532_driver);
 
 MODULE_AUTHOR("Junjie Cao <caojunjie650@gmail.com>");
+MODULE_AUTHOR("Serhii Hatsan <serhiihatcan@gmail.com>");
 MODULE_DESCRIPTION("DRM driver for Novatek NT36532 based MIPI DSI panels");
 MODULE_LICENSE("GPL");
